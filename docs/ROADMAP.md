# Roadmap

Estado vivo. Actualizar en cada hito.

## v0.1.0 · Esqueleto

- [x] Repo git inicializado en `C:\Users\Míchel\suaas`.
- [x] Next.js 16 App Router, sin Tailwind.
- [x] Tokens del DS de sd.michelvalles.com en `globals.css`.
- [x] Login HUD (réplica del estilo adams/uoc) con cookie `auth_suaas`.
- [x] `proxy.ts` que protege todo lo no público.
- [x] Stubs de `lib/supabase.ts` y `lib/gateway.ts`.
- [x] Dashboard placeholder con status cards (Supabase / AI Gateway / Login).
- [x] Reglas operativas en `CLAUDE.md` (docs vivas + bump + deploy + commit).
- [x] Base de conocimiento (`docs/CONOCIMIENTO-USUARIOS-SINTETICOS.md`).

## v0.2.0 · Datos + primer agente

- [x] Esquema inicial en `supabase/migrations/0001_initial.sql`: `profiles`, `targets`, `runs`, `messages`, `metrics`.
- [x] `lib/profiles.ts` con `ProfileInputSchema` (zod) y CRUD vía service role.
- [x] `lib/runs.ts` con `createRun`, `appendMessage`, `listMessages`, `nextTurn`.
- [x] `lib/prompts.ts` con `buildSystemPrompt` (vignette + negative prompts).
- [x] `/profiles` (lista), `/profiles/new` (Server Action), `/profiles/[id]` (detalle con chat).
- [x] `/api/chat` (POST) con `generateText` sobre `DEFAULT_MODEL`. Persiste turnos en `messages`.
- [x] `components/app-shell.tsx` (header con nav + footer con versión).
- [x] **Bloqueante operativo resuelto**: Supabase provisionado desde el Marketplace de Vercel y migraciones aplicadas. La app opera contra base real en producción.

## v0.3.0 · Talker-Reasoner

- [x] `lib/agents.ts` con `reason()` (Opus + `generateObject` + `ReasonerPlanSchema`) y `talkStream()` (Sonnet + `streamText`).
- [x] `/api/chat` reescrito: Reasoner síncrono → persist turno `reasoner` con `meta.plan` → Talker en streaming → persist turno `talker`.
- [x] Protocolo NDJSON: frames `meta`/`delta`/`done`/`error` separados por `\n`.
- [x] `ChatPanel` lee el stream, muestra el texto progresivo y un `<details>` "Razonamiento" colapsable bajo cada turno con tono, esfuerzo, intent, barreras y plan.
- [x] Métrica `effort_ratio` por run, calculada como media de `effort` sobre turnos `reasoner`. Upsert en `metrics`.

## v0.3.x · Fixes post-lanzamiento

- [x] `0.3.1`: typo / bump menor.
- [x] `0.3.2`: `/api/chat` llama a `markRunFinished(runId, "done" | "error")` antes del frame final. Hasta 0.3.1 los runs OK quedaban con `status='running'` y `finished_at=null`.

## v0.4.0 · Test de claridad de 5 segundos

- [x] Migración `0002_five_second.sql`: tabla `five_second_responses` (vista normalizada) + índice por `run_id`. No cambios estructurales en `targets`.
- [x] `lib/targets.ts` (`TargetInputSchema`, `FiveSecondPayloadSchema`, CRUD, `resolveOgImage`).
- [x] `lib/experiments/five-second.ts` con `probeProfile`, `judgeComprehension`, `runFiveSecondTest`.
- [x] UI: `/targets` (lista), `/targets/new` (URL o upload), `/targets/[id]` (hero + runs previos + multi-select de perfiles + "Lanzar test").
- [x] `/api/runs/five-second` con orquestador paralelo (chunks de 5 en flight).
- [x] `/experiments/five-second/[runId]` con tabla sortable por perfil, summary (mean_clarity, mean_comprehension, n), top barreras.
- [x] `components/result-bar.tsx` reutilizable.
- [x] Modelos: Reasoner (Opus) para `probeProfile` (multimodal con imagen), Sonnet para `judgeComprehension`.
- [x] Métricas en `metrics`: `mean_clarity`, `mean_comprehension`, `n` (`comprehension_p50` no aporta sobre `mean_comprehension` con N pequeño, lo dejamos fuera).

Decisiones aplicadas: LLM-as-judge para fuzzy-match (no embeddings), batch sync con límite de 20 perfiles, dos modos de captura (URL → `og:image` server-side o upload a `data:` URL).

## v0.5.0 · Simulación de embudo

- [x] **Definición + persistencia de embudos** (v0.5.0): migración `0003_funnels.sql` (`funnels` + `funnel_steps`), `lib/funnels.ts` con `FunnelInputSchema` / CRUD, `/funnels` (lista), `/funnels/new` (form dinámico, 2..12 pasos, modos URL o upload por paso), `/funnels/[id]` (secuencia ordenada con hero por paso). Nav "Embudos" en el shell.
- [x] **Run con perfil(es) recorriendo el embudo paso a paso** (v0.5.2): migración `0004_funnel_runs.sql` (`runs.funnel_id` + `funnel_step_responses`), `lib/experiments/funnel.ts` con `probeFunnelStep` (Reasoner multimodal con memoria de pasos previos) y `runFunnelTest` (orquestador en chunks de 5), `/api/runs/funnel`, `LaunchPanel` en `/funnels/[id]`, `/experiments/funnel/[runId]` con summary, dropoff por paso, top fricciones agregadas y tabla por perfil con drill-down expandible.
- [x] **Detección de fricción**: cada respuesta de paso captura `effort` 0..1, `intent_match` 0..1 y un array de `friction` textual. Las métricas agregadas (`completion_rate`, `mean_effort`, `mean_intent_match`) y el dropoff por paso permiten ver dónde se rompe el embudo.
- [x] **Almacenamiento de uploads en Vercel Blob** (v0.6.1): `lib/blob.ts` con `uploadDataUrlToBlob` (helper que sube `data:` URLs a Blob con sufijo aleatorio y devuelve la URL pública). Acciones de creación de target y de funnel convierten los uploads en URLs https permanentes en lugar de meter el `data:` URL en jsonb (la base no se llena de blobs base64). Store `suaas-uploads · store_1yLEHreMdAwe3V6D` creado en `iad1`. Si `BLOB_READ_WRITE_TOKEN` no está disponible, hace fallback al `data:` URL para no romper desarrollo local.

## v0.7.0 · A/B tests, Copy resonance, Pricing

Tres módulos nuevos de experimentación en una sola release.

- [x] **A/B tests**: tabla `ab_tests` (target_a_id, target_b_id, hypothesis), tabla puente `ab_test_runs` (variant A|B) + columna `runs.ab_test_id`. Lanzar un A/B dispara DOS runs 5s en paralelo con el mismo set de perfiles vía `runAbTest` (reutiliza `runFiveSecondTest`). Rutas: `/ab` lista, `/ab/new`, `/ab/[id]` detalle + LaunchPanel, `/experiments/ab/[abTestId]` con comparativa lado a lado + ganador.
- [x] **Copy resonance**: tablas `copy_decks` + `copy_blocks` + `copy_responses` + `runs.copy_deck_id`. `lib/experiments/copy.ts` con `reactToBlock` (Sonnet via `generateObject`, sin imagen). Cada perfil reacciona a 2..10 bloques con sentiment, clarity 0..1, persuasion 0..1, would_click y critique. Rutas: `/copy`, `/copy/new`, `/copy/[id]`, `/experiments/copy/[runId]` (bloques ordenados por persuasión + barra de sentimiento + drill-down por perfil).
- [x] **Pricing**: tablas `pricing_offers` + `pricing_prices` (2..8 niveles) + `pricing_responses` + `runs.pricing_offer_id`. `lib/experiments/pricing.ts` con `reactToPrice`. Cada perfil reacciona a cada precio con would_buy, willingness_to_pay 0..1, perceived_value 0..1, critique. Métricas: curva de demanda, sweet_spot por revenue esperado (price × buy_rate). Rutas: `/pricing`, `/pricing/new`, `/pricing/[id]`, `/experiments/pricing/[runId]`.
- [x] Componente reutilizable `components/profile-launch-panel.tsx` para multi-select de perfiles + lanzamiento. Reemplazará progresivamente los LaunchPanel específicos en futuras iteraciones.
- [x] Sidebar con 3 entradas nuevas: A/B tests (`Split`), Copy (`MessageSquareText`), Pricing (`Tag`).
- [x] `/diag` y `/api/diag` añaden las 8 tablas nuevas.
- [x] Telemetría de tokens extendida a `copy_resonance` y `pricing_react` scopes.

Aplicar `0006_ab_copy_pricing.sql` en Supabase antes de crear el primer registro de cualquiera de los tres.

## v0.8.0 · Explorador de perfiles + reutilización de selección

Refactor grande del módulo de perfiles para tratarlo como software (no como lista web).

- [x] **Vista grid/tabla** con toggle por comodidad. La tabla muestra demografía + Big Five compacto; el grid muestra nombre, ocupación, dots O·C·E·A·N y tag de hover. Cambio en `components/profile-explorer.tsx`.
- [x] **Filtros**:
   - Rangos numéricos por edad y por cada rasgo Big Five (0..1).
   - Texto "contiene" multipalabra (AND, normalizado, sin acentos): busca en name, occupation, gender, income_band, geo, barreras COM-B y source. Explícitamente NO incluye backstory.
   - Filtros activos resaltan el botón. Reset disponible.
- [x] **Selección**: checkbox por fila, contador "X sel." en toolbar, "Seleccionar todos los visibles" y limpiar selección.
- [x] **Acciones por fila** (modo manage): ver (`/profiles/[id]`), editar (`/profiles/[id]/edit`) y eliminar (con `confirm` + endpoint `DELETE /api/profiles/[id]`).
- [x] **Backstory en hover**: tarjeta y fila tienen un tooltip absoluto con la backstory completa. No aparece en filtros ni columnas.
- [x] **Validación robusta** en create + edit. `lib/profile-form.ts` con `ProfileFormSchema` reutilizable: edad entero 18-99, Big Five 0..1, género enum, backstory ≥20. Mensajes con etiquetas en castellano.
- [x] **Fix selector género**: `colorScheme: dark` + `background: var(--ink-900)` en options. Antes se veía blanco sobre blanco según el SO/navegador.
- [x] **Página de edición** `/profiles/[id]/edit` que reutiliza `<ProfileForm>` compartido.
- [x] **ProfileLaunchPanel rehecho** sobre `<ProfileExplorer mode="picker">`: ahora todos los sitios donde se selecciona perfil (targets, funnels, copy, pricing, A/B) heredan filtros, vista grid/tabla y hover de backstory.

Aplazadas a futuras subversiones (por alcance):
- ~~v0.8.2~~ → reasignada a la retirada del hover de backstory (ver más abajo).
- v0.8.3: generación automática de 48 perfiles vía LLM.

## v0.8.1 · Importador y exportador CSV de perfiles

- [x] `lib/csv.ts`: parser/serializer CSV isomórfico sin dependencias (RFC 4180 simplificado, soporte `,` y `;`, comillas dobles, BOM, EOL `\n`/`\r\n`).
- [x] `lib/profile-csv.ts`: schema CSV con 16 columnas (`PROFILE_CSV_HEADERS`), `profileToCsvRow` (export) y `validateCsvRow` (import, reutiliza `ProfileFormSchema` para que las reglas sean idénticas al formulario web).
- [x] **Exportar**: botón en la toolbar de `/profiles`. Exporta los **seleccionados** si hay selección o los **visibles** según los filtros aplicados. Descarga client-side, UTF-8 + BOM (Excel lo respeta).
- [x] **Importar**: `/profiles/import` con dropzone, detección automática de separador, parseo cliente, validación fila a fila con preview de estado (verde/rojo) y mensaje específico por fila errónea. Server action `importProfilesAction` inserta sólo las filas válidas; tope de 500 filas por import.
- [x] Columnas desconocidas del CSV se ignoran con aviso, y faltantes generan un error claro en la validación (no se importa la fila).
- [x] Listas COM-B (capability/opportunity/motivation) se serializan con `;` interno para sobrevivir al separador `,` del CSV.

## v0.8.2 · Retirada del hover de backstory

- [x] Quitado el tooltip flotante de backstory en el grid y la tabla (ruido visual). La backstory sigue accesible abriendo el detalle del perfil. Limpieza de `.profile-hover*` en `globals.css`.

## v0.8.3 · Generación de perfiles vía LLM

- [x] `lib/seed-profiles.ts`: 50 seeds curados en castellano (demografía española variada, desde estudiantes a jubilados, urbano/rural, diferentes ocupaciones y barreras COM-B). Schema `SeedOutputSchema` con validación zod estricta. Generación con Reasoner (Opus) via `generateObject`.
- [x] `streamSeededProfiles(n)`: orquestador async generator que produce eventos `started` / `progress` / `error` / `done`. Inserta cada perfil inmediatamente para no perder trabajo si algo falla a mitad.
- [x] `/api/profiles/seed` (POST): endpoint con stream NDJSON. Cuerpo `{ n: 48 }`.
- [x] `/profiles/seed`: UI con selector de N, botón "Generar", barra de progreso y log en vivo (verde por perfil creado, rojo por error con el seed que lo originó).
- [x] Botón "Generar con LLM" añadido a la toolbar de `/profiles` junto a "Importar/Exportar CSV".
- [x] Telemetría: cada generación registra en `gateway_usage` con `scope: reasoner_chat` y meta `kind: seed_profile`.

## v0.9.0 · Auditoría de seguridad / estabilidad / limpieza

Hardening del login y manejo robusto de migraciones pendientes, sin cambios funcionales.

- [x] **Seguridad · login**: `getAccessPassword()` ya no cae a `michel101` en producción si falta `ACCESS_PASSWORD`; devuelve `null` y bloquea cualquier intento de login. Nueva función `verifyAccessPassword()` con `crypto.timingSafeEqual` constant-time (defensa frente a timing attacks). `app/api/auth/route.ts` migrado al helper.
- [x] **Estabilidad · migraciones pendientes**: nuevo helper `isMissingTableError(err)` que detecta el código PostgREST `PGRST205`. Las listas de `/ab`, `/copy`, `/pricing` ya no crashean si falta `0006_ab_copy_pricing.sql`: muestran un componente `<MigrationNeeded>` con el nombre exacto de la migración a aplicar.
- [x] **UX · AI Gateway sin enlazar**: copy más claro en `/tokens` explicando que los runs siguen funcionando vía OIDC y que sólo la consulta de saldo necesita "Connect to project" en el dashboard.
- [x] **Limpieza · código muerto**: eliminado `getBrowserClient` de `lib/supabase.ts` (no se usaba en ningún sitio; SUAAS opera todo desde server con service role). Eliminado `app/profiles/actions.ts` completo (sus dos exports `deleteProfileAction` / `deleteProfileAndRedirect` fueron reemplazados por `DELETE /api/profiles/[id]` en v0.8.0).
- [x] **Docs**: árbol en `docs/PROYECTO.md` sincronizado con la realidad.

## v0.6.0 · App shell tipo software

- [x] Sidebar lateral izquierdo (240px en desktop, overlay colapsable en móvil con botón hamburguesa) reemplazando el header con nav. Iconos `lucide-react` por entrada.
- [x] Footer global con badges de estado (Supabase ready / AI Gateway ready) en verde cuando están configurados.
- [x] Home reescrita como presentación del software (feature cards + cómo funciona en 3 pasos), no como dashboard de estado.
- [x] Nueva ruta `/diag`: vista visual del estado del esquema y configuración (mismo dato que `/api/diag` pero renderizado).
- [x] Nueva ruta `/tokens`: créditos del AI Gateway + acumulado interno por modelo y por scope. Soportada por migración `0005_gateway_usage.sql` y `lib/usage.ts`. Llamadas instrumentadas: probe_5s, judge_5s, probe_funnel, reasoner_chat, talker_chat.

## v0.10.x · Pulido visual y layout

- [x] **v0.10.0**: home rediseñada (presentación del software con feature cards + 3 pasos), cursiva display en backstory, grid uniforme en cards.
- [x] **v0.10.1**: armonía visual en `/profiles/[id]` (detalle de perfil).
- [x] **v0.10.2**: `PageHeading` deja de tener `maxWidth` interno; título y descripción ocupan todo el ancho del container.
- [x] **v0.10.3**: botón hamburguesa del sidebar móvil pasa a círculo flotante sin texto, oculto cuando el menú está abierto.
- [x] **v0.10.4**: tabla de perfiles oculta la columna OCEAN en mobile para no desbordar.
- [x] **v0.10.5**: quitada la franja de KPI de la home (redundante con `/diag` y `/tokens`).

## v0.11.x · Sembrador de ejemplos en los 4 módulos

- [x] **v0.11.0**: `/seed-examples` con endpoint POST `/api/seed/examples`. Crea en una pasada: copy deck (4 variantes CRO de Flat 101), pricing offer (Flat 101 Lab con 4 niveles), A/B test y embudo de Stripe. Opcionalmente lanza runs sobre N perfiles aleatorios. `lib/seed-examples.ts` con funciones puras y `pickRandomProfileIds(n)`.
- [x] **v0.11.1**: home con más respiro, nota redundante eliminada en `/profiles`.

## v0.12.x · Resilencia frente a schema cache

- [x] **v0.12.0**: `createRun` defensivo (sólo inserta columnas con valor no-null para sobrevivir a un schema cache de PostgREST desactualizado tras una migración). Defaults de seed corregidos (BBVA bloqueaba bots → N26+Revolut; ficha de Filmin sin og:image → Notion). `/tokens` reescrito con KPI grandes y desglose Prompt/Completion.
- [x] **v0.12.1**: URLs definitivas para los seeds (Vercel vs Netlify para A/B, Stripe en 4 pasos para el embudo) tras probarlas desde el sandbox. Schema cache sigue requiriendo `NOTIFY pgrst, 'reload schema';` manual.
- [x] **v0.12.1 (docs)**: simplificación del copy del hero de la home.

## v0.13.x · Papelera con soft delete

- [x] **v0.13.0**: migración `0007_trash.sql` añade `deleted_at timestamptz` a las 5 entidades (targets, funnels, ab_tests, copy_decks, pricing_offers). Botón papelera en cada card. Página `/trash` lista los elementos borrados con acciones "Restaurar" y "Eliminar definitivamente". `lib/trash.ts` orquesta soft delete / restore / hard delete.
- [x] **v0.13.1**: fallback defensivo cuando la migración 0007 no está aplicada (las queries se reintentan sin `is("deleted_at", null)`).
- [x] **v0.13.2**: fix en targets para que vuelvan a aparecer en la papelera.
- [x] **v0.13.3**: detalle de funnels/ab/copy/pricing no rompe si faltan columnas `runs.X_id`.
- [x] **v0.13.4**: descripción debajo de Big Five y Barreras COM-B en el detalle de perfil.
- [x] **v0.13.5**: `/diag` y `/api/diag` auditan también las columnas críticas de `runs` (no sólo la existencia de tablas).

## v0.14.x · Tablas más útiles y nomenclatura UI

- [x] **v0.14.0**: runs previos como cards (componente `RunsPreviousGrid` en `components/runs-previous.tsx`) reemplazando el patrón tabla. Aplica en las 5 páginas de detalle.
- [x] **v0.14.1**: nombres de perfil clicables en las tablas de resultados de experimentos.
- [x] **v0.14.2**: renombrado UI **Targets → Claridad 5s**. Sidebar con icono `ScanEye`, copy actualizado en `/targets/*`, `/ab/*` y home. Rutas y tabla `targets` se mantienen como nombre técnico interno.

## v0.15.x · Plantilla unificada de listado

- [x] **v0.15.0**: nueva plantilla `EntityListView` + `EntityCard` para los 5 listados (claridad, embudos, ab, copy, pricing) con búsqueda + ordenación.
- [x] **v0.15.1**: explorer de perfiles con sort ascendente/descendente al click en cabecera, paginación cada 30 items en grid y tabla, rediseño armonioso (Big Five sparkbars en vez de números crudos).

## v0.16.x · UX y QA del formulario de perfil

- [x] **v0.16.0**: `EntityCard` muestra fecha arriba y kind abajo. Filtros y ordenación en listados.
- [x] **v0.16.1**: inputs Big Five aceptan 2 decimales (step 0.01) en `/profiles/new`, `/profiles/[id]/edit` y los filtros del explorer. Antes el navegador rechazaba `0,68` con "los más próximos son 0,65 y 0,70".
- [x] **v0.16.2**: más aire en buscador, toolbar de listados y entity cards (padding/gap aumentados a través de `entity-list.tsx` y `globals.css`).
- [x] **v0.16.3**: imágenes saneadas antes de enviar a Anthropic (multimodal); `resolveOgImage` más permisivo con sitios que sirven og:image relativo o sin prefijo http.
- [x] **v0.16.3 (ui)**: remaqueta de las 4 plantillas de RUN con más aire entre secciones y stats.

## v0.64.1 · Titulares sin itálica

- [x] **v0.64.1 · Retirada de la itálica de los titulares**: el patrón heredado «título en display itálica» (venía de DM Serif) desentonaba en la grotesca nueva. Se quita `font-style: italic` de los 23 puntos donde se emparejaba con la display font (titulares, nombres de entidad, labels y valores métricos): `.entity-card__title` en `globals.css` y los estilos inline de home, explorer de perfiles, GEO, momentum, funnels, pricing, propuesta, tokens, papelera y las cards comparativas. Los `h1/h2/h3` heredan el peso 700 del selector de elemento; a los titulares que son `span`/`Link` se les fija `fontWeight: 700`. La cursiva se preserva solo donde es recurso de cita: backstory (`.backstory-box`, textarea, OG card), respuestas VoC simuladas y narrativa de intención/JTBD. Regla resultante: **itálica = cita, nunca titular**.

## v0.64.0 · Refresh de identidad (Fase 1): Hanken Grotesk, sin serif

- [x] **v0.64.0 · Fundación tipográfica del rediseño «oscuro editorial»**: arranca el rediseño (registro Stripe/Linear, coherente con flat101.es) validado antes en preview (`/diseno-preview`). Toda la app pasa a una única grotesca, **Hanken Grotesk**: se jubilan DM Serif Text y Nunito Sans (`layout.tsx`). En `globals.css`, `--font-sans`/`--font-mono`/`--font-display` apuntan a Hanken y el fallback del display pasa de serif (`Times New Roman`) a sans, que era la fuga por la que los titulares salían con serifa cuando la fuente no cargaba; `.h1`/`.h2`/`.display` a peso 700 con tracking negativo. El acento `--accent-500` se fija en el amarillo de marca de Flat 101, **#F9CB0D** (también en `console-banner`). La cursiva editorial del backstory y las citas se conserva (clase propia), ahora en Hanken itálica. Verificado en producción en tema oscuro y claro (home + perfil): sin serif, sin regresiones, contraste AA intacto. Doc en `SISTEMA-DISENO.md`. Pendiente del rollout: superficies cálidas, retirada de eyebrows, composición editorial por pantalla, chat estilo WhatsApp en el panel real y borrado de `/diseno-preview`.

## v0.63.6 · Contraste por habitus de la fricción simbólica

- [x] **v0.63.6 · Prueba de dos perfiles documentada**: la lente `social_friction` (v0.63.5) se prueba con dos perfiles de habitus opuesto ante la misma táctica extractiva («teléfono por PDF») y el contraste se incorpora a la base de conocimiento (`CONOCIMIENTO-USUARIOS-SINTETICOS.md` §5.6). Lucía Sáez (arquitecta, media-alta, capital cultural alto) da `social_friction` 0,75 con fricción de tipo «ofensa a su criterio experto»; Noelia Cárdenas (auxiliar administrativa, media-baja, informada en foros) da 0,60 con fricción de tipo «precariedad material y dignidad». Ambas rechazan la táctica: la lente lee habitus (distinta cualidad e intensidad), no «fricción alta para todos», y la fábula «capital cultural bajo = dócil» no se compone. Con caveats honestos: una sola ejecución (LLM estocástico) y Noelia no es un perfil de baja alfabetización, así que la hipótesis del dark pattern sobre perfiles dóciles sigue sin probar.

## v0.63.5 · Lente de fricción simbólica en el Reasoner (capa macrosociológica)

- [x] **v0.63.5 · `social_friction` en el Reasoner del chat**: a raíz de una revisión sociológica externa, el plan del Reasoner (`lib/agents.ts`) emite por turno un objeto `social_friction` (`intensity` 0..1, `trigger`, `habitus_note`) que, más allá de las barreras funcionales COM-B, evalúa disonancia de clase, exclusión cultural o pérdida de legitimidad leyendo el habitus y el capital cultural del perfil (Bourdieu). Se pinta en el panel «Razonamiento» del chat y, si es alta, el Reasoner la refleja en `plan` para que el Talker la module (sin vía de inyección nueva). Verificado en vivo: la táctica extractiva «teléfono por PDF» da 0,7 con lectura de habitus y arrastra el momentum a `drifting/decelerating`; un saludo neutro da 0,2. Documentado con estatus honesto en `CONOCIMIENTO-USUARIOS-SINTETICOS.md` §5.6 y `PERFILES-CALIBRADOS.md` §3.4: es **solo del chat** (no de los tests por lotes), **declarativo** (medición simulada, sin validar) y **simbólico, no relacional** (la dimensión de prueba social / Granovetter sigue sin operacionalizar). También se incorporan los reencuadres de la revisión: racionalidad limitada de Simon en §8.1, trazabilidad como etnografía computacional en §5 y guardarraíles como garante de la validez ecológica en §8.3.

## v0.63.4 · Saneado de coherencia de toda la carpeta docs/

- [x] **v0.63.4 · Pasada de coherencia documental**: auditoría multiagente de los 17 documentos de `docs/` (barrido mecánico de enlaces, referencias a ficheros y anchors, más lectura semántica cruzada). Enlaces, ficheros citados y anchors resuelven todos; la doctrina era coherente, pero la capa factual de `PROYECTO.md` y `DESARROLLO.md` seguía anclada en v0.34 y no reflejaba v0.62.0-v0.63.3. Corregido: cabeceras de versión a v0.63.3 en `PROYECTO.md`, `GRAVITY-MODEL.md`, `PERFILES-CALIBRADOS.md` y `SIGUIENTE-PASO.md`; listas de migraciones extendidas a 0029 en `PROYECTO.md` y `DESARROLLO.md`; árboles de `lib/`/`app/api/` con guardrails, rag y crons; contradicciones de RLS y de scopes de telemetría (`seed_profile`, `batch_intent`, `rag_embed`) resueltas; slugs de modelo con punto; tabla `brand_document_chunks` y columnas `brand_id` añadidas; citas `archivo:línea` del dossier de perfiles re-verificadas tras el desplazamiento por los imports de guardrails/rag; `README.md` lista ya `GRAVITY-MODEL-IVI.md`; los logs fechados (`AUDITORIA-SEGURIDAD.md`, `VERTEX-AI-VALORACION.md`) anotados inline sin falsear su fecha (RAG implementado, `CRON_SECRET` creado, PoC de inyección ejecutado).

## v0.63.3 · Scripts de prueba fuera del árbol de producción

- [x] **v0.63.3 · Retirada de los scripts de prueba**: los scripts puntuales de verificación en vivo (guardarraíles, inyección de texto y RAG, ejecutados el 19-jul-2026 contra el gateway real y con los resultados ya incorporados a `CONOCIMIENTO-USUARIOS-SINTETICOS.md` §8.3-8.4) se sacan del árbol de producción: eran artefactos de una sesión de pruebas, no código de la app. La última fila del roadmap vuelve a cuadrar con `lib/version.ts`.

## v0.63.2 · Saneado del doc fundacional con fuentes verificadas

- [x] **v0.63.2 · Reescritura de `CONOCIMIENTO-USUARIOS-SINTETICOS.md`**: todas las cifras y citas rastreadas a su fuente primaria y verificadas (las tres cifras de validación a Yun et al. 2025, la encuesta N=1.093 a User Interviews 2023, Talker-Reasoner a Christakopoulou et al. 2024, Concordia a Vezhnevets et al. 2023), con las atribuciones erróneas del original corregidas (Ishii, Stegbauer, Bouzit). Añadidas la trazabilidad atributo → respuesta con una transcripción real (perfil Lucía) y las pruebas en vivo de guardarraíles, anti prompt injection y RAG ejecutadas contra el gateway real el 19-jul-2026 con las funciones de producción (§8.3-8.4).

## v0.63.1 · Monitorización GEO periódica descartada

- [x] **v0.63.1 · Monitorización GEO periódica descartada (decisión de producto)**: no se construirán re-runs programados del GEO Tester con cron; cuando haga falta comparar, se relanza el análisis a mano (la advertencia de varianza inter-run de `GEO-PRUEBA-IVI.md` sigue vigente al interpretar resultados). Sincronizado con `SIGUIENTE-PASO.md` y con los huecos GEO de `GRAVITY-MODEL.md`.

## v0.63.0 · RAG de Cerebro con pgvector

- [x] **v0.63.0 · RAG de Cerebro**: los documentos de marca no sensibles se trocean (~1.000 caracteres con solape), se vectorizan vía AI Gateway (`openai/text-embedding-3-small`, 1536 dims, 0,02 $/1M tokens, verificado en vivo con failover a Azure) y se recuperan por similitud coseno en lugar del volcado íntegro de 30.000 caracteres. Migración **`0029_rag_cerebro.sql`** aplicada vía MCP: pgvector 0.8.0, tabla `brand_document_chunks` (RLS activado, índice HNSW), función `match_brand_chunks` (excluye `sensitive` también en SQL y con `revoke execute` a anon: defensa en profundidad) y columnas `brand_id` en `geo_analyses` y `momentum_challenges`. `lib/rag.ts` nuevo (chunker por párrafos con límite duro, `indexDocument`/`indexBrand`, `searchBrandChunks`, `buildBrandContextRag` con fragmentos neutralizados por guardrails); indexado automático al guardar o editar documentos en Cerebro y botón «Reindexar marca» para el backfill; scope de telemetría `rag_embed`. GEO recupera por segmento (`jtbd + query`) y Momentum por trigger, ambos con fallback al modo legado (marca sin indexar o gateway caído) y sin cambio para los análisis históricos. Ahorro estimado: ~75% de los tokens de contexto de marca por run de GEO. Los documentos `sensitive` siguen sin salir jamás al gateway: no se indexan, no se devuelven, no se concatenan.

## v0.62.0 · Hardening integral (auditoría multiagente)

- [x] **v0.62.0 · Guardarraíles anti prompt injection**: `lib/guardrails.ts` nuevo (delimitación con fence + instrucción de inertado, neutralización de delimitadores, caps por fuente; sin blacklists semánticas) aplicado a las 8 superficies mapeadas: respuestas de motores GEO con búsqueda web (inyección indirecta desde internet, la más expuesta), documentos de Cerebro, copy y queries del anunciante, `main_promise` del 5s, backstories y barreras del perfil (saneado inline), textos abiertos del onboard público e `IMAGE_TEXT_GUARD` en los prompts multimodales. RLS activado en `app_settings`, `brands` y `brand_documents` (migración **`0027`**, advisory crítico de Supabase). Contraseña y contenido de «Conceptos pendientes» de `/gravity` movidos del bundle al servidor (`POST /api/gravity/unlock`, `timingSafeEqual`, env `GRAVITY_PASSWORD`).
- [x] **v0.62.0 · Crons de operación**: `vercel.json` con keep-alive diario del Supabase Free (`/api/cron/keepalive`; el 19-jul el proyecto se pausó por inactividad y bloqueó 7 deploys con «Resource provisioning failed» además de degradar la app) y reaper anti-zombi (`/api/cron/reaper`, mitiga B-01/B-03 liberando los `running` de más de 1 hora; los runs de campaña quedan reanudables). Ambos exigen `Bearer CRON_SECRET` (fail-closed) y `/api/cron/` queda exento de cookie en `proxy.ts`. GEO y Momentum fijan `updated_at` al pasar a `running`. **Pendiente del usuario: crear `CRON_SECRET` en Vercel** (comando en `DESARROLLO.md`).
- [x] **v0.62.0 · Pasada de optimización**: lint reparado (roto desde Next 16: `next lint` eliminado; ESLint flat nativo destapó 18 hallazgos nunca vistos), N+1 de perfiles eliminado en los 5 runners (`loadRunProfiles`, una query en vez de hasta 20), `chunks` desduplicado en `lib/experiments/shared.ts`, Momentum paralelizado en chunks de 5 con cap de 20 perfiles y desbloqueo de zombis (10 min), `upsertMetric` atómico (migración **`0028`**: dedupe + índice único), scope propio `seed_profile`, `RunKind "ab_test"` muerto eliminado (el enlace via `abTestId` va en el `createRun`), `batch-intent` paralelo, medias `null` en bloques de copy sin datos, sweet spot de pricing `null` si nadie compra, placeholder «(contexto general)» filtrado en los framings de Display y legacy, y limpieza menor (`lib/utils.ts` huérfano, separadores del system prompt que no llegaban al modelo, `meta.model` del talker).
- [x] **v0.62.0 · Valoración de orquestación**: `docs/ORQUESTACION-VALORACION.md` nuevo. Conclusión: n8n no procede como orquestador de los agentes (no ejecuta el TypeScript del AI SDK, no levanta el `maxDuration`, duplica secretos privilegiados); hoja de ruta con Vercel Cron (hecho) y Workflow DevKit para la durabilidad de los runners (siguiente iteración).

## v0.61.15-16 · Documentación del Gravity Model y dossier de perfiles

- [x] **v0.61.15 · Revisión integral de la doc del Gravity Model** (workflow de 10 agentes, 1,16M tokens de lectura): `docs/PERFILES-CALIBRADOS.md` nuevo (anatomía del perfil, fundamento académico por componente con autores canónicos, tabla de fidelidad por módulo, autores que no citar, críticas metodológicas anticipadas y guion de síntesis; dirigido a una experta en Behavioural Economics y sociología); `GRAVITY-MODEL.md` verificado contra el código (migraciones corregidas, los dos modos de Momentum, estado real del vector momentum como medición declarativa, las dos semánticas de `behavior_class`, ancla académica en la teoría de campo de Lewin, huecos GEO, nomenclatura SUAAS/Gravity); `GRAVITY-MODEL-IVI.md` fechado y ligado a los 15 perfiles reales; comentarios contradictorios del mapeo HEXACO→OCEAN corregidos en `lib/hexaco.ts`.
- [x] **v0.61.16 · Saneado del doc fundacional**: `CONOCIMIENTO-USUARIOS-SINTETICOS.md` llegó de una exportación con el markdown escapado (no renderizaba); desescapado completo, título duplicado eliminado, em-dashes fuera, números en formato español y nota editorial (cifras sin fuente recuperada, Monte Carlo marcado aspiracional, contraste teoría↔implementación remite a `PERFILES-CALIBRADOS.md`).

## v0.57.0 · Retratos fotorrealistas de perfiles (AI Gateway + Blob)

- [x] **v0.57.0 · Retrato generado por IA por perfil**: columna `profiles.avatar_url` (migración **`0024_profile_avatar.sql`**, aplicada vía MCP), `lib/avatar.ts` con `generateImage` del AI SDK contra el gateway (modelo `google/imagen-4.0-generate-001`, ~0,04 $/retrato, configurable con `SUAAS_AVATAR_MODEL`) y subida a Vercel Blob reutilizando `uploadDataUrlToBlob` (la cara queda fijada; cada generación daría una distinta). El prompt se construye desde la demografía y el tono emocional (neuroticismo) y **no incluye el nombre** para minimizar parecidos casuales con personas reales; la UI etiqueta siempre «retrato generado por IA». `POST /api/profiles/[id]/avatar` (maxDuration 120; 402 accionable si el gateway se queda sin créditos), botón generar/regenerar en el detalle del perfil y miniaturas en las cards y la tabla del explorer. Scope de telemetría `profile_avatar` con `meta.est_usd` (la facturación de imagen es por unidad, no por tokens). Generados los retratos de los 15 perfiles de la investigación IVI.

## v0.56.0 · GEO Tester con motores reales (Claude, ChatGPT, Perplexity)

- [x] **v0.56.0 · Sondas reales en el GEO Tester**: el módulo deja de simular. La query de cada segmento se lanza tal cual contra 3 motores reales vía AI Gateway: **Claude** (modelo Anthropic + tool server-side `web_search` con `userLocation` España), **ChatGPT** (tool `web_search` de la Responses API de OpenAI) y **Perplexity** (Sonar busca y cita por sí mismo). Un segundo paso (`generateObject` sobre `DEFAULT_MODEL`, scope nuevo `geo_analysis`) analiza cada respuesta real (brand_mentioned, brand_position, visibility_score, recommendation_tone, key_claims, missing_attributes) sin inventar nada. `results` v2: por segmento un array `engines` con respuesta, citas (URLs deduplicadas de `result.sources`) y métricas o `error` por motor (un motor caído no tumba el análisis). Los análisis históricos (v1, `simulated_response`) se siguen renderizando como legado. Runner: segmentos en pares (6 sondas concurrentes máx.; una sonda con búsqueda tarda 30-50 s) y `maxDuration = 300` en `/api/geo/run`. UI del detalle: pestañas de motor funcionales (AI Overview y Gemini quedan anunciados como próximos), citas como chips clicables, modelo usado y visibilidad por motor; el resumen pasa a visibilidad media por motor. Smoke test real contra el gateway con las 3 sondas OK (Claude 30 citas, Perplexity 8, ChatGPT 7).
- [x] **Selector de modelos por motor en `/tokens`**: sección «Modelos del GEO Tester» con un select por proveedor (Anthropic: Haiku 4.5 / Sonnet 4.6 / Opus 4.8 · Perplexity: Sonar / Sonar Pro · OpenAI: GPT-5.4 mini / GPT-5.4 / GPT-5.5), para alternar entre pruebas ligeras y análisis reales. Persiste en la tabla nueva `app_settings` (migración **`0023_app_settings.sql`**, aplicada vía MCP) bajo la clave `geo_engine_models`; el runner la lee en cada lanzamiento y los slugs guardados se validan contra el catálogo (`lib/geo-engines.ts`, slugs verificados contra `GET /v1/models` del gateway). Sin la migración: defaults y aviso en la sección.
- [x] **Costes y dependencias**: tarifas nuevas en `model-pricing.ts` (sonar, sonar-pro, gpt-5.5, gpt-5.4, gpt-5.4-mini, opus-4.8) + fallbacks por familia; la estimación del botón «Analizar» usa los modelos elegidos (3 sondas + 3 análisis por segmento) y suma ~0,01 $/sonda de cuota de búsqueda (10 $/1.000 en Anthropic y OpenAI, 5-14 $/1.000 en Perplexity). Dependencias nuevas: `@ai-sdk/anthropic` y `@ai-sdk/openai` (solo aportan las tool factories; las llamadas siguen yendo por el gateway) y `ai` actualizado a 6.0.202 para deduplicar `provider-utils`.

## v0.55.0 · Canal TikTok Ads (vídeo in-feed / carousel / spark)

Investigación multiagente verificada adversarialmente contra fuentes oficiales de TikTok (Business Help Center `ads.tiktok.com/help/article/*`, Marketing API «Create Ad» y el YAML del SDK oficial) el 2026-06-12, e implementación del canal completo.

- [x] **v0.55.4 · Renombrado de producto: «usuarios sintéticos» → «perfiles calibrados»**: en toda la superficie visible (login, home, `/profiles`, `/targets`, formulario y validaciones de campañas, nota metodológica, metadata global) y también en inglés (la home y el pie del onboard dejan «Synthetic Users as a Service»; SUAAS queda como marca pura sin expansión del acrónimo). El onboard pasa de «gemelo sintético» a **«gemelo digital»** (página, resultado, imagen OG y modal de compartir). Los prompts del LLM se alinean (momentum, seed de perfiles, seed de brief, onboard y los jueces de campañas y claridad 5s). `/propuesta` unifica «perfiles modelados» → «perfiles calibrados». Se conservan intactos: la base teórica `CONOCIMIENTO-USUARIOS-SINTETICOS.md` (término académico) y sus referencias, los registros históricos (este ROADMAP, auditoría, migraciones, SIGUIENTE-PASO) y la terminología de mercado de `Plan-venta.md`. Regla nueva en `CLAUDE.md`: el término de producto es «perfiles calibrados».

- [x] **v0.55.1 · Fixes de la revisión adversarial del módulo TikTok**: una revisión multiagente del commit v0.55.0 (3 lentes + verificación adversarial de cada hallazgo) confirmó 9 bugs, corregidos: (1) **ALTA**: el select «Rol en el anuncio» de creatividades TikTok mostraba un valor derivado en vez de `c.role`, y los roles residuales al cambiar de canal/estrategia (generic de Google, card/cover de Meta) viajaban intactos al servidor bloqueando el submit con la UI contradiciendo el error; ahora el select refleja el rol real y `normalizeCreativesFor` renormaliza las creatividades al entrar en una estrategia TikTok (tabs de canal y de estrategia). (2) Cambiar el rol a «Foto de perfil» no reseteaba el kind: un vídeo-avatar satisfacía la validación de «1 vídeo con miniatura»; ahora el cambio de rol a imagen resetea el medio y el `superRefine` excluye `logo_square` del conteo de vídeos (y `creativesForModel` excluye el avatar en vídeo/spark). (3) `apply-tiktok-setup.mjs` podía aplicar la 0021 DESPUÉS de la 0022 y revertir los checks; ahora reaplica la última si una anterior llegó tarde. (4) Seed IVI no atómico: `createOrReuse` dedupe por nombre para que un reintento tras la migración no duplique las campañas ya creadas. (5) `NO_EMOJI_REGEX` ampliada (estrella U+2B50, relojes, flechas, ‼, ™…) y «@» añadido al check de caracteres prohibidos del ad text. (6) Spark: la versión ideal, la síntesis y el export capaban a 100c «sin emojis» el caption que el propio formato modela a 150c con emojis; ahora Spark usa 150c y permite emojis/hashtags en las tres rutas. (7) El render del carousel y `creativesForModel` cuentan tarjetas con el mismo filtro que la validación (`role card` + `kind image`): el número de imágenes anunciado al perfil cuadra con las adjuntas. (8) El contador de tarjetas permitía «36 / 35» (el +1 del avatar contaba como tarjeta): el cap ahora cuenta tarjetas, no creatividades. (9) El @usuario de Spark se declaraba obligatorio sin estarlo: `required` en el input y check en el `superRefine`.

- [x] **v0.55.0 · Canal TikTok Ads**: el canal `tiktok` deja de estar «Próx.» y modela el **formato del anuncio como estrategia** (`tiktok_video` vídeo in-feed de subasta, `tiktok_carousel` carousel de 2-35 imágenes con música obligatoria, `tiktok_spark` post orgánico promocionado), porque en TikTok el tipo de campaña es el objetivo y no determina los campos. Lo específico del canal vive en `campaigns.channel_spec` con el **discriminador `network: "tiktok"`** (las filas de Meta no lo llevan; `ChannelSpec` pasa a union `MetaSpec | TikTokSpec` con helpers `metaSpecOf`/`tiktokSpecOf` en todos los call sites): objetivo (`reach`/`traffic`/`video_views`/`community_interaction`/`app_promotion`/`lead_generation`/`sales`, el selector 2025-2026 tras la fusión de Website Conversions + Product Sales), **1..5 textos de anuncio** (rotan como el `ad_text_list` de Smart+: el runner muestrea UNO por impresión con `sampleTikTokCombination` y lo registra en `shown_descriptions` como Meta), `identity_handle` (@usuario) y `music_name`. Caps verificados: **ad text 1-100c latinos** sin emojis ni «#»/«{ }» (validados en zod con regex; Spark hereda el caption del post: 150c y emojis permitidos), **display name 40c técnicos / 20 visibles** (aviso ámbar), carousel **2-35 imágenes** (mejor CTR con 3 o 7-9, vertical 720x1280) con **música obligatoria**, vídeo 9:16 ≥540x960 recomendado, 5-60s (mejor 21-34s), ≤500 MB. CTA obligatoria de la **lista cerrada localizada al español** (25 opciones de landing page; «Book now» y «Pre-order now» comparten «Reservar ahora»); migración **`0022_tiktok_ads.sql`** (pendiente de aplicar; ver `scripts/apply-tiktok-setup.mjs`): strategy check con los 3 formatos, CTAs de TikTok en el check de `cta` y, defensivamente, `channel_spec` + `company_name` 75c por si la 0021 no consta. Runner: renders propios (`renderTikTokVideoSnippet`/`Carousel`) con identidad @usuario + «Patrocinado», caption truncado a ~2 líneas con «[más]» (4 líneas es el máximo técnico), fila de música («Promoted music» si no se nombra) y CTA que aparece a los pocos segundos; framing del feed «Para ti» con las **ventanas reales del targeting** (vídeos/hashtags 7-15 días, creadores 30 días) y after-click por objetivo; versión ideal y síntesis con límites TikTok (gancho 40c / caption 100c). Form: tabs con TikTok activo (3 formatos), secciones de objetivo, identidad (display name + @ + foto de perfil 98x98 como creatividad `logo_square`), variantes de texto, música y CTA; `TikTokAdPreview` en vivo: marco 9:16 oscuro fijo con tabs «Siguiendo | Para ti», columna derecha de iconos (avatar con «+», corazón, comentarios, guardar, compartir y disco girando con la clase nueva `.spin-slow`), @usuario + chip «Patrocinado» + caption con «más» + fila de música y botón CTA, con dots/contador/flechas en carousel y botón «Seguir» en Spark. Export nuevo `?format=tiktok` (Ad Text 1..5, Display Name, Identity, CTA, Música, URL; filas Original + SUAAS ideas) y botón propio en la página de resultados. Detalle de campaña con chips de objetivo/feed y sección «Anuncio de TikTok». Seed IVI ampliado a **7 estrategias** (la séptima: `tiktok_video` con el spot real, objetivo lead_generation y CTA «Reservar ahora»; multiplicador del seed 14 → 16).

## v0.54.0 · Canal Meta Ads (formatos single / carousel / collection)

Investigación multiagente verificada adversarialmente contra fuentes oficiales de Meta (Ads Guide `facebook.com/business/ads-guide/update/*`, Marketing API `asset_feed_spec` y `placement-targeting`, Help Center) el 2026-06-12, e implementación del canal completo.

- [x] **v0.54.6 · `RemoveIconButton` compartido y aplicado a los 5 forms `/new`**: el icono de papelera del form de campañas se factoriza a `components/remove-icon-button.tsx` (prop `label` para aria/title específicos) y sustituye a los botones «Eliminar» de copy (bloques), pricing (niveles), embudos (pasos, junto a ↑/↓) y GEO (segmentos, que usaba un pill propio). Misma regla en todos: el icono solo se renderiza cuando la fila puede eliminarse.
- [x] **v0.54.5 · Iconos de papelera en el form de nueva campaña**: los botones «Eliminar» de las filas repetibles (búsquedas, textos principales, titulares, descripciones) y de las creatividades pasan a un icono plano de papelera (`RemoveIconButton`, local del form, espejo de la variante `inline` de `SendToTrashButton`), que además solo se renderiza cuando la fila puede eliminarse (antes era un botón deshabilitado). Menos `btn-pill` en una página que ya acumula muchos.
- [x] **v0.54.3 · Chip de Meta en morado #9D00FF**: lo diferencia de los azules de Google y LinkedIn (el azul corporativo #1877F2 se confundía con ellos de un vistazo). `CHANNEL_BRAND` y la nota del DS actualizados.
- [x] **v0.54.2 · Chips de canal con color de marca**: los chips de canal del grid de campañas usan el color corporativo de cada red (`CHANNEL_BRAND` en `components/channel-icon.tsx`: Google #4285F4, Meta #1877F2, LinkedIn #0A66C2, TikTok #FE2C55; X queda neutro porque su marca es blanco/negro) y el chip de estrategia pasa de accent a neutro del tema. Excepción de color documentada en `SISTEMA-DISENO.md → Reglas`.
- [x] **v0.54.1 · Grid de campañas con chips de canal y estrategia**: cada card del listado `/campaigns` muestra en cabecera los chips con logo del canal (ChannelIcon) y de la estrategia (StrategyIcon, en accent), clonando el patrón de la sección «Canal y estrategia» del detalle; la fecha baja al pie junto al número de runs. `EntityCard`/`EntityListItem` ganan props opcionales `badges` (sustituye a la fecha en cabecera), `dateInFooter` y `searchExtra` (mantiene canal y estrategia buscables aunque ya no estén en los stats del pie); el resto de listados no cambia.
- [x] **v0.54.0 · Canal Meta Ads**: el canal `meta` deja de estar «Próx.» y modela el **formato del anuncio como estrategia** (`meta_single` imagen/vídeo único, `meta_carousel` secuencia de 2-10 tarjetas, `meta_collection` portada + tiles de producto), porque en Meta el tipo de campaña es el objetivo ODAX y no determina los campos. Lo específico del canal vive en la columna nueva `campaigns.channel_spec` (jsonb, **migración `0021_meta_ads.sql`, pendiente de aplicar**): objetivo ODAX (`awareness`/`traffic`/`engagement`/`leads`/`app_promotion`/`sales`), placement de simulación (Feed de Facebook, Feed de Instagram, Stories, Reels, Threads, Estados de WhatsApp; matriz oficial formato × placement validada en zod), 1..5 **textos principales** y enlace visible. Caps de copy con la distinción oficial **máximo técnico** (asset_feed_spec: primary 1.024c, headline/description 255c, 5 variantes por campo) vs **recomendado visible** (125/40/30; avisos ámbar en el form sin bloquear). Tarjetas de carousel y tiles de colección como creatividades con roles nuevos `card`/`cover` y campos `card_headline` (45c rec) / `card_description` (18c rec). CTA obligatoria de la lista cerrada de Meta en castellano («Más información», «Comprar», «Registrarte»…). Identidad = nombre de página de Facebook (75c; el check de BD sube de 25 y zod mantiene 25 para Google). Runner: renders propios por formato y placement (`renderMetaSingleSnippet`/`Carousel`/`Collection`) con **truncado ecológico** del primary text por placement (feed/Stories 125c, Reels 72c, Threads 160c + «Ver más»), framing por placement y objetivo (`metaFraming`), muestreo de UNA combinación texto principal + titular + descripción por impresión (`sampleMetaCombination`, flexible ad format real) con el primary text mostrado registrado en `shown_descriptions`, ranking por asset con el kind nuevo `primary_text`, versión ideal y síntesis con límites de Meta (40/125). Form: tabs de canal con Meta activo, estrategias filtradas por canal (`CHANNEL_STRATEGIES`), secciones de objetivo/placement/identidad/textos principales, previews en vivo de feed, 9:16 con safe zones oficiales (14% superior, 35% inferior, 6% laterales), carousel deslizable y colección con cuadrícula. Export nuevo `?format=meta` (Primary Text 1..5, Headline 1..5, Description 1..5, CTA, URLs; filas Original + SUAAS ideas) y el botón de la página de resultados elige Ads Editor o Meta Ads según la estrategia. Detalle de campaña con chips de objetivo/placement, textos principales con aviso de truncado y tarjetas con su titular.

## v0.41.x+ · Campañas: requisitos por estrategia según las specs oficiales de Google

Adaptación de los requisitos de cada estrategia a las páginas oficiales de specs de Google Ads (answers 17092074, 17091269, 17091270, 17091672 y la especificación del feed de Merchant Center 7052112), extraídas y verificadas contra las fuentes el 2026-06-11.

- [x] **v0.53.0 · Nueva lógica de precios de los paquetes**: (1) el presupuesto de IA incluido sube a Starter 30 $, Pro 70 $ y Agency 200 $/mes (antes 10/30/100). (2) La calibración pasa a ser **opcional** (deja de presentarse como setup obligatorio): en las tarjetas «Calibración opcional: X € (una vez)». (3) Cada paquete incluye la **generación de 50 perfiles calibrados según el target del proyecto** (feature nueva en los tres tiers). En la calculadora interna, el coste de IA se deriva del presupuesto incluido por paquete (30/70/200 $ × EUR) en vez de un valor manual; el desglose muestra «IA (presupuesto incluido por paquete)» y «Calibración opcional (una vez)». Márgenes actualizados (~86-90 %). Docs `Plan-venta.md` (tablas de tiers) al día.
- [x] **v0.52.3 · Landing `/propuesta`: nav sin anclas, IA como presupuesto adicional**: (1) el nav deja de mostrar las anclas (en móvil el toggle por scroll parpadeaba); queda estático con logo + Gravity + CTA, sin lógica de scroll. (2) En la calculadora, el «uso de IA %» pasa a ser un presupuesto de IA adicional por instancia (€/mes), que suma al coste; el desglose muestra «IA (N × X€)». (3) Título de la gráfica simplificado a «Coste de infraestructura por instancia».
- [x] **v0.52.2 · Landing `/propuesta`: pulido móvil, nav inteligente y calculadora what-if interna**: (1) nav extraído a client (`nav.tsx`): contraído deja sólo logo + Gravity + CTA («Ver paquetes»); las anclas aparecen al hacer scroll up y se ocultan al bajar, con sincronizado inicial. Logo compartido en `flat-logo.tsx`. (2) Botones del hero en una línea sin desbordar en móvil (320-360px). (3) Slider del módulo de IA acotado a 10-200 $ (default 50) y tablas sin scroll horizontal (`tableLayout: fixed`, columnas 44/28/28). (4) Mensaje del motor de IA reescrito coloquial. (5) Calculadora reconvertida en herramienta interna protegida (sólo vista interna, sin tarificador cliente): escenario what-if por mezcla de paquetes (cuántos Starter/Pro/Agency, 0 incluido), uso de IA %, gestión; desglose de infraestructura (Supabase base + Micro, Vercel seat + compute, IA, gestión) y gráfica SVG de amortización del coste por instancia (12 + 35/N) con marcador del escenario actual. Revisión adversarial pasada.
- [x] **v0.52.1 · Landing `/propuesta`: ritmo visual, full-bleed y módulo de IA interactivo**: (1) layout a sangre completa, fuera todos los `max-width` (contenedor y caps de texto), con bandas (`Band`) de ancho total y padding propio. (2) Apartado «Qué incluye» en blanco (`.surface-paper`) y cierre en banda oscura (`.surface-feature`) para romper el esquema. (3) El módulo de coste de IA pasa a client (`ai-module.tsx`): se quitan las columnas `$/Mtok entrada/salida` y se añade un slider con input de texto asociado (5-1.000 $/mes) que recalcula en vivo cuántos runs entran con cada modelo de cada proveedor.
- [x] **v0.52.0 · Landing `/propuesta` pública con marca Gravity, metodología y gate interno**: iteración comercial sobre la v0.51. (1) Marca de producto «Gravity» (constante `BRAND`); el copy deja de decir «usuarios sintéticos» y usa «perfiles modelados». (2) Ruta `/propuesta` ahora PÚBLICA (en `PUBLIC_PATHS` de `proxy.ts`): se comparte con clientes sin login. (3) La vista interna de la calculadora (rentabilidad bruta y neta) se protege con contraseña (`michel101`, override `LANDING_PASSWORD`): `lib/landing-auth.ts` + `POST /api/propuesta/access` (cookie httpOnly, timingSafeEqual) + login en el footer (`unlock-form.tsx`); sin desbloquear la calculadora sólo muestra el tarificador. (4) El Gravity Model es la metodología comercial, con la animación orbital de los tres planos (Construcción/Aceleración/Valor, clases `gm-center`/`gm-dot`). (5) Menú de anclas sticky, hero sin `max-width` y estilo flat101.es (datos grandes en amarillo, CTAs con flecha, alto contraste). Nota: el gate es de presentación (la calculadora corre en el navegador); para confidencialidad total de los márgenes habría que calcular en servidor. Opción anotada: subdominio `landing.suaas.flat101.business` apuntando a esta ruta (paso manual de DNS/Vercel).
- [x] **v0.51.0 · Landing comercial `/propuesta` con calculadora y módulo de coste de IA**: página de venta para directivos, detrás del login (los márgenes no deben ser públicos). No usa el nombre interno: marca de producto en la constante `BRAND` («Prisma», placeholder editable). Reutiliza el sistema de diseño (tokens, fuentes, logo de Flat 101 inline con `currentColor`) sin AppShell, presentable en tema claro y oscuro. Secciones: hero de valor, anclaje vs research real (4.000-10.000 € y 2-4 semanas), módulos, paquetes (Starter/Pro/Agency + Enterprise + piloto), módulo de coste de IA (tabla por proveedor Anthropic/OpenAI/Gemini/Perplexity con €/run y runs por 30 $/mes, precios verificados 2026-06-11) y cierre. `lib/landing-pricing.ts` (catálogo de modelos, tiers, `runCostEur`, `runsForBudget`, `BUDGET_REF_USD`) alimenta page y calculadora. `app/propuesta/calculator.tsx` (client): tarificador + rentabilidad bruta y neta con selector de proveedor/modelo que mueve el COGS, runs incluidos capados por el budget de la key, y toggle cliente/interno (por defecto cliente, modo seguro: oculta márgenes). Revisión adversarial pasada: corregido un NaN al vaciar el precio.
- [x] **v0.50.0 · Coste estimado en dólares en cada botón que consume el gateway**: cuarta fase del blindaje, ahora visible en la UI. Infraestructura nueva: `lib/model-pricing.ts` (tarifas $/MTok por modelo con fallback por familia, `usdForTokens`, `formatUsd` es-ES) y `lib/estimate.ts` (`estimateAction`, `estimateManyUsd`, `partsForKind`: fórmulas de llamadas por acción derivadas de los runners, preciadas con las medias históricas de `gateway_usage` con split prompt/completion nuevo en `getScopeAverages`, y fallback conservador sin histórico). `GET /api/estimate/run?kind=&profiles=&perProfile=&judge=1` sustituye a `/api/estimate/campaign` y cubre los 6 módulos del panel. Superficies: `ProfileLaunchPanel` muestra «coste ~X $» junto a tokens y segundos (los 6 forms pasan `estimateEndpoint`: 5s, copy con bloques, pricing con precios, funnel con pasos como cota superior, A/B x2, campañas con judge), los botones «Analizar» de GEO (por segmentos) y Momentum (por perfiles) llevan el coste en el label, el chat muestra el coste por mensaje bajo el formulario (reasoner Opus + talker Sonnet, con aviso de que el historial encarece), «Generar N perfiles» multiplica el coste Opus por perfil y `/seed-examples` enseña coste por tarjeta y el total de «Crear los 8 y lanzar» (multiplicadores espejo del contenido sembrado, coste fijo + variable por perfil derivado de dos estimaciones). `/onboard` queda fuera a propósito (los visitantes anónimos no deben ver costes internos). Las cifras son orientativas (medias empíricas; funnel es coste máximo por el abandono).
- [x] **v0.49.1 · Fixes de la revisión adversarial del blindaje de costes**: una revisión multiagente del diff v0.47.2..v0.49.0 encontró 1 hallazgo alto y 3 medios, corregidos: (1) `getTokensLast24h` y `getUsageSummary` paginan con `.range()` (PostgREST corta a 1.000 filas por petición: bajo carga el presupuesto se quedaba ciego y los totales de `/tokens` se congelaban). (2) `downscaleIfNeeded` encadena `.rotate()` antes de `.resize()`: sharp descarta el EXIF al recomprimir y una foto de móvil con Orientation distinta de 1 llegaba tumbada al modelo. (3) `getScopeAverages` excluye las filas `meta.failed=true` (tokens null contaban como 0 y deflactaban la media que alimenta `/api/estimate`, infraestimando justo tras un incidente). (4) `executeCampaignRun` re-comprueba `assertBudget` cada 25 combinaciones (el gate de la ruta solo se evalúa a la entrada; un run de 200 combos admitido al 99% del presupuesto lo desbordaba entero): lo saltado queda en la métrica nueva `n_skipped_budget` y es retomable. Además la fila failed de la síntesis se registra pegada a la llamada LLM (un fallo de persistencia posterior duplicaba la contabilidad). Pendientes anotados de la revisión (no bloqueantes): telemetría de fallos solo cubre campañas (chat/5s/funnel/onboard siguen ciegos al gasto fallido), el reintento del worker re-factura las fases que sí funcionaron, GIF exento del redimensionado, sin tope de bytes en la descarga de imágenes y `seed-profiles` registra bajo el scope `reasoner_chat`.
- [x] **v0.49.0 · Imágenes multimodales redimensionadas antes de enviar al modelo**: tercera fase del cierre del incidente de la cuota. `resolveImageForApi` (lib/image-source.ts) gana `downscaleIfNeeded` con `sharp` (dependencia explícita nueva, ^0.34.5; la 0.35.0 no expone tipos en sus exports): jpeg/png/webp se reducen a un máximo de 1024px de lado largo antes del base64, manteniendo el formato (un screenshot png no gana artefactos jpeg); GIF pasa intacto (animación; el modelo solo ve el primer frame). El modelo cobra por píxel (~ancho×alto/750 tokens, con reescalado server-side a 1568px): las creatividades y landings llegaban a resolución completa del origen y ahora pagan ~1.000-1.400 tokens como mucho por imagen. Si sharp no puede procesar la imagen se envía el original (mejor grande que ninguna). Aplica a todos los flujos multimodales (campañas, 5s, funnels).
- [x] **v0.48.0 · Gate de presupuesto en todas las rutas que consumen LLM y contabilidad de batch-intent**: segunda fase del cierre del incidente de la cuota. `budgetGate` (SUAAS_DAILY_TOKEN_BUDGET, 429) deja de cubrir solo los 8 endpoints de runs y pasa a cubrir también `/api/chat` (2 llamadas por turno, una en Opus), `/api/seed/examples` cuando va a consumir LLM (launch > 0 o brief; era la acción más cara de la UI y la puerta del incidente), `/api/profiles/seed` (Opus), `/api/profiles/batch-intent` y `/api/onboard/submit` (única ruta pública con LLM; responde un 429 genérico sin filtrar detalles internos, regla A-02). Además `batch-intent` registra por fin su consumo (`recordUsage`, scope nuevo `batch_intent`): era el único call site del gateway invisible para `/tokens` y el presupuesto. Auditoría actualizada: C-02 mitigado, C-03 cerrado.
- [x] **v0.47.2 · Runner de campañas íntegro en Sonnet y telemetría de llamadas fallidas**: consecuencia directa del incidente de la cuota del AI Gateway (la key agotó su budget de 3$ el 2026-06-11, en gran parte por los probes Opus multimodales rotos del seed IVI, facturados x2 por el reintento y sin rastro en `gateway_usage`). (1) `probeCampaignSnippet` deja de usar `REASONER_MODEL` también en texto puro: todo el runner de campañas va en `DEFAULT_MODEL` (Sonnet); el diferencial de calidad no justificaba el precio (5$/25$ vs 3$/15$ por MTok) y el incidente zanja la comparación Opus vs Sonnet que estaba pendiente como tarea operativa. Opus queda solo en chat (reasoner), onboard y seed de perfiles. (2) Telemetría de fallos: helper `usageFromError` (extrae el `usage` de `NoObjectGeneratedError`, que factura tokens aunque el parse falle) y `recordUsage` con `meta.failed=true` en los catch del probe, landing, juez, versión ideal y síntesis. Las llamadas fallidas dejan de ser invisibles para `/tokens` y para el presupuesto diario.
- [x] **v0.47.1 · Fix de los runs de campaña con creatividades (Opus + generateObject + imágenes) y error visible**: los 6 runs IVI cerraban en `error` con 0 respuestas. Causa: desde v0.45.1 toda campaña adjunta imágenes al probe (Search incluye el logo) y `probeCampaignSnippet` usaba `REASONER_MODEL` (Opus 4.7), que con `generateObject` + imágenes devuelve el JSON envuelto en XML de tool-call que el AI SDK no parsea (mismatch ya documentado en `five-second.ts`, que por eso usa Sonnet y funciona). Fix: el probe elige modelo según el contenido (con imágenes `DEFAULT_MODEL`, texto puro sigue con el Reasoner) y `judgeLandingMatch` (siempre multimodal) pasa a `DEFAULT_MODEL`; `gateway_usage.model` y `meta.model_*` reflejan el modelo real. Además el runner persiste el último error de combinación en `runs.params.last_error` (limpio a null si el cierre no tuvo fallos), la página del run lo muestra cuando el estado es `error` («Por qué falló el run») y `RunProgress` ofrece «Retomar» también cuando el run falló con 0 respuestas (antes solo con parciales, así que un fallo total dejaba la página sin botón).
- [x] **v0.47.0 · Seed «Campañas IVI: las 6 estrategias» con runs en segundo plano**: `seedCampaignStrategiesExample()` crea 6 campañas reales sobre ivi.es (clínicas de reproducción asistida), una por estrategia implementada, con copy fiel a sus claims públicos (tasas hasta un 23% superiores a la media, 35 clínicas, financiación TIN 0%, Plan IVI Baby) dentro de los límites de cada formato, imágenes reales de su web (og:image, dos banners 340x192), su favicon como logo y su spot de YouTube «La noticia de mi vida» para Video y PMax; Shopping usa como producto el estudio de fertilidad (190 EUR). Kind nuevo `campaign_strategies` en `/api/seed/examples` (excluido del «sembrar todos» y del plan por brief): con `launch > 0` selecciona perfiles de 28 a 45 años (`pickProfileIdsByAgeRange`, la audiencia natural del ejemplo, completando con aleatorios), prepara los 6 runs en el request y los ejecuta en `after()` en paralelo; si la función se corta, cada run ofrece «Retomar». Tarjeta nueva en `/seed-examples` con un enlace al run de cada estrategia.
- [x] **v0.46.0 · Formulario reorganizado, previews por estrategia y poda post-migraciones**: con las migraciones 0019 y 0020 aplicadas por el operador (confirmado; `/diag → Tracking de migraciones` lo refleja): (1) **formulario de campaña reorganizado**: los campos comunes a todas las estrategias (nombre interno, brief, mensaje pretendido, URL final y landing) van primero, antes de elegir canal y estrategia, de modo que cambiar de estrategia nunca borra ni esconde lo ya rellenado; los específicos (empresa, producto, queries, titulares, titular largo, descripciones, CTA, creatividades) van después del selector. (2) **Fix de los desplegables**: regla global `select option { background: var(--surface-panel); color: var(--text-strong) }` en `globals.css` (el popup nativo en Windows/Chromium renderizaba fondo claro con texto del tema: blanco sobre blanco). (3) **Vista previa específica por estrategia**: Search gana empresa + logo (como el SERP real), Display y PMax comparten el banner (PMax con nota de composición), y se añaden `FeedAdPreview` (tarjeta de Discover para Demand Gen), `VideoAdPreview` (pre-roll con miniatura y «Saltar anuncio») y `ShoppingAdPreview` (ficha de producto con foto, precio, marca y disponibilidad). (4) **Poda de fallbacks legacy**: `createCampaign` pasa a un insert directo (fuera los retries de `channels` pre-0011, `strategy` pre-0013, `intended_message` 0019 y `product` 0020), `normalizeCampaign` pierde el mapeo `channel` → `channels[]` (la 0019 dropeó la columna) y `listCampaigns`/`getCampaign` pierden los retries inertes de `deleted_at`. Los fallbacks de lectura desde `meta` en el runner se conservan: las filas históricas guardaron ahí sus valores.
- [x] **v0.45.4 · Shopping corregido contra la especificación del feed (7052112)**: el `ProductSchema` gana los dos atributos que faltaban de los obligatorios/condicionales básicos: `id` (obligatorio, máx. 50c, «usa el SKU») y `mpn` (máx. 70c; en el feed real es obligatorio solo si el producto no tiene GTIN). Formulario y action con los campos nuevos; comentario de la columna en la 0020 (aún sin aplicar) actualizado. Con esto Performance Max queda verificado sin cambios (la re-extracción confirmó todos sus números) y las 5 estrategias coinciden con las tablas literales de sus fuentes.
- [x] **v0.45.3 · Video corregido contra la tabla literal de la spec (17091270)**: la tabla de text assets de Video action campaigns lista Headline (30c), **Long headline (90c)**, Description (90c), CTA (10c) y Final URL, sin declarar cantidades ni obligatoriedad. Se añade el titular largo (opcional, solo in-feed) al formulario y al render del pre-roll; la CTA pasa de exigida a opcional manteniendo el límite de 10c; los mensajes de duración pasan a «recomendada 10 segundos o más» (la columna de la fuente es «Recommended length», no un mínimo obligatorio). Decisión de modelado documentada: el módulo lleva 1 titular + 1 descripción + 1 vídeo por anuncio (estructura del formato; la fuente no lo declara).
- [x] **v0.45.2 · Demand Gen corregido contra la tabla literal de la spec (17091672)**: la tabla marca la CTA como obligatoria (1, «Automated», ✓): pasa de opcional a exigida en el `superRefine` y el select del formulario lo indica. Descripción de la estrategia corregida: la imagen vertical de la tabla es 4:5 (el 9:16 solo existe en los vídeos, opcionales, 10-60s recomendados, hasta 3 por orientación) y el mínimo del logo es 144x144. El resto ya coincidía con la fuente (1-5 titulares de 40c con al menos uno de 30c o menos, 1-5 descripciones de 90c, empresa de 25c, landscape + square + logo obligatorios, vertical 4:5 opcional).
- [x] **v0.45.1 · Search corregido contra la tabla literal de la spec (17092074)**: re-extracción fila a fila con doble verificación. La tabla del bloque «Responsive search ads» dice 1-15 titulares (30c) y 1-4 descripciones (90c), ambos obligatorios: se retiran los mínimos de 3/2 que venían de otra página (7684791), quedando mínimo 1 de cada. El bloque «Business information» marca obligatorios el nombre de empresa (25c, 1) y el logo 1:1 (1, mín 128x128): Search los exige ahora (`company_name` + creatividad `logo_square`), el formulario muestra empresa, roles de creatividad y CTA opcional (el bloque «Ad assets» la lista como opcional automatizada) también en Search, el render del SERP incluye al anunciante con su logo, y el sembrador deriva empresa y logo (favicon 128px) del dominio. El bloque «Ad assets» (formatos asset-based con titulares de 25c y cantidades 1-20) queda documentado como no modelado: sus límites contradicen a los del RSA y el módulo simula el RSA.
- [x] **v0.45.0 · Shopping implementada según la spec del feed de Merchant Center (7052112)**: `isStrategyImplemented` pasa a excluir solo `app`. En Shopping no se redacta el anuncio: la campaña guarda el producto del feed en la columna nueva `campaigns.product` (jsonb, **migración `0020_shopping.sql`, pendiente de aplicar**; puede pegarse en la misma sesión que la 0019). `ProductSchema`: título (150c), descripción (5.000c), precio con divisa ISO 4217, disponibilidad (in_stock/out_of_stock/preorder/backorder), marca (70c) y GTIN (8-14 dígitos) opcionales, condición. Los mínimos globales de titulares/descripciones bajan a 0 (la 0019 sin aplicar se reajusta) y pasan al superRefine por estrategia; Shopping exige producto completo, al menos 1 búsqueda y la imagen principal (≥500x500). El formulario oculta el copy y muestra la sección «Producto»; el detalle pinta la ficha en lugar del snippet SERP. Runner: `renderShoppingSnippet` (ficha en el carrusel con título, marca, precio, disponibilidad y la foto principal) y framing de búsqueda con intención de compra; la versión ideal pasa a «título de ficha» con truncado a 150c. `createCampaign` lanza `MigrationPendingError(0020)` si se crea una campaña shopping sin la columna (el resto de estrategias sigue funcionando sin ella). Diag: `campaigns.product` → 0020 en `CRITICAL_COLUMNS` y `0020_shopping.sql` en `KNOWN_MIGRATIONS`.
- [x] **v0.44.0 · Video implementada según la spec oficial (17091270)**: `isStrategyImplemented` gana `video` (Video action campaign). Validación: exactamente 1 titular (30c), 1 descripción (90c), CTA de texto libre de máximo 10 caracteres (en Video la CTA no sale de la lista: el formulario cambia el select por un input con contador y el check de `cta` de la 0019, aún sin aplicar, se relaja a «set de Google Ads o ≤ 10c») y 1 vídeo (YouTube o subido) de 10s o más, cuya duración no es verificable desde la app. Sin nombre de empresa, logo ni imágenes sueltas (la spec no los contempla en este formato). Runner: `renderVideoSnippet` (pre-roll saltable: anunciante + titular + descripción + CTA + la miniatura como único visual conocido antes de saltar) y framing de YouTube con la query como interés de actividad. Sin muestreo (1 titular + 1 descripción únicos).
- [x] **v0.43.0 · Demand Gen implementada según la spec oficial (17091672)**: `isStrategyImplemented` gana `demand_gen` (subformato imagen única; carousel y vídeo quedan sin modelar). Particularidad clave: titulares de **40 caracteres** (el cap global de zod sube a 40 y el límite de 30 pasa al superRefine de las demás estrategias; el formulario adapta el contador por estrategia). Validación oficial: 1..5 titulares (al menos uno de 30c o menos, regla de Ad Strength), 1..5 descripciones, nombre de empresa (25c) y landscape (1.91:1) + square (1:1) + logo (1:1); portrait/vertical y CTA opcionales (automatizada por defecto). Sin titular largo (solo existe en el subformato vídeo). Runner: `renderDemandGenSnippet` (tarjeta patrocinada en feed con 1 titular + 1 descripción muestreados por impresión) y framing de Discover/YouTube feed.
- [x] **v0.42.0 · Performance Max implementada según la spec oficial (17091269)**: `isStrategyImplemented` gana `pmax`. Validación con la combinación mínima oficial del grupo de recursos: 3..15 titulares (30c, al menos uno de 15c o menos), titular largo (90c), 2..5 descripciones, nombre de empresa (25c), CTA y creatividades con landscape (1.91:1) + square (1:1) + logo square (1:1); portrait, logo landscape y vídeo opcionales. El formulario reutiliza el set de assets de Display vía `assetStrategy` (empresa, titular largo, CTA, roles de creatividad, preview de banner) con las queries como «Señales de audiencia (opcional)». Runner: `renderPmaxSnippet` (anunciante + titular largo + 1 titular corto + 1 descripción + CTA muestreados por impresión, como combina Google) y framing multi-superficie (Discover, Gmail, YouTube, red Display). El muestreo por impresión usa `sampleRsaCombination(1, 1)` y alimenta el ranking por asset igual que Search. Limitación documentada: el modelo guarda 1 titular largo (la spec admite hasta 5).
- [x] **v0.41.0 · Search según la spec oficial RSA**: Google exige mínimo 3 titulares y 2 descripciones para crear un RSA (la tabla de specs admite desde 1, pero el editor de creación exige 3/2; validamos lo exigible). `superRefine` de search gana `headlines >= 3`; el formulario marca los 3 primeros titulares como obligatorios en Search, impide bajar de 3 filas y lo explica con una nota. Los caps existentes (30c por titular, 90c por descripción, máximo 15/4) ya coincidían con la spec.

## v0.40.0 · Campañas: muestreo RSA y ranking por asset (plan CAMPANAS-PLAN-MEJORA, release 6 de 6)

- [x] **v0.40.0 · Muestreo de combinaciones RSA reales y rendimiento por asset**: en Search/Google el persona deja de ver los 15 titulares y 4 descripciones a la vez (nadie ve eso: validez ecológica rota) y pasa a ver UNA combinación muestreada de 3 titulares + 2 descripciones (`sampleRsaCombination`, RNG determinista mulberry32 sembrado por `profileId + query`: el mismo perfil ve la misma combinación al repetir el run con la misma muestra, exposición equilibrada en expectativa). La combinación se persiste en `shown_headlines`/`shown_descriptions` (columnas de la 0019, fallback conjunto a `meta`). El summary gana `byAsset` (n, intent medio y delta vs la media del run por titular y descripción, solo filas donde el asset estuvo presente) y la página de resultados la sección «Rendimiento por asset» ordenada por intent con las filas de n < 5 atenuadas (muestra insuficiente). **Cambio de semántica documentado en PROYECTO.md: las respuestas evalúan una combinación, no el conjunto; los runs anteriores a v0.40 no son comparables con los posteriores.** Display y feed mantienen su render. Cierra el plan de mejora de campañas (16 de 16).

## v0.39.x · Campañas: profundidad de análisis (plan CAMPANAS-PLAN-MEJORA, release 5 de 6)

- [x] **v0.39.3 · Síntesis accionable «Qué cambiar» por run**: `synthesizeRecommendations` en el motor (una llamada `DEFAULT_MODEL` por run, tolerante a fallos, scope `campaign_synthesis`): hallazgos clave, hasta 3 titulares y 2 descripciones listos para pegar (refinando las mejores versiones ideales, truncado suave a 30/90, nunca `.max` fatal) y cómo desactivar cada barrera top. Alimentada con el summary, las queries más débiles y el top 10 de ideales por intent; instruida en castellano y sin em-dash. Se persiste mergeada en `runs.params.recommendations` (jsonb, sin migración) y la página de resultados la pinta como sección destacada «Qué cambiar» (validada con zod al leer). Cierra la release v0.39.
- [x] **v0.39.2 · Tabla de respuestas interactiva y token `--serp-link`**: nueva `responses-table.tsx` (client) en la página de resultados de campaña, clon estructural de la de five-second: filas perfil × canal × query ordenables por intent/claridad/credibilidad/match/comprensión, filtros por query y canal, chip de conducta del Gravity Model y drill-down por fila (oferta percibida, razonamiento, barreras, crítica de landing y versión ideal). Sustituye los `<details>` server-rendered, que no escalaban a 200 filas. Token nuevo `--serp-link` (azul SERP desaturado en oscuro, azul de enlace clásico en claro, fijo en `.theme-dark-fixed`) con clase `.serp-link`: sustituye los 7 `rgba(132,192,255,…)` hardcodeados de resultados, detalle y formulario, documentado en `SISTEMA-DISENO.md`. Fleco aplazado: reutilizar el `DisplayAdPreview` del formulario en el detalle de campañas Display (hoy enseña preview SERP también para banners).
- [x] **v0.39.1 · `behavior_class` del Gravity Model en campañas**: `SnippetEvalSchema` gana el enum `behavior_class` (optima/fuga/repesca) adaptado a ads, colocado tras `barriers` y antes de los scores (la clase sale del razonamiento, no del número). Persistencia en la columna de la 0019 con fallback conjunto a `meta` (faltan juntas), lectura tolerante en `listCampaignResponses` (null en filas antiguas), `behavior_counts` global y por query/canal en el summary y sección «Conducta predicha (Gravity Model)» en resultados con desglose por query. Check de consistencia interna: `behavior_inconsistencies` cuenta respuestas cuya clase contradice su intent (fuga con ≥ 0,5 u óptima con < 0,3) y se muestra como nota cuando aparece. Alinea el módulo con five-second y la narrativa de la home.
- [x] **v0.39.0 · Juez neutral de comprensión del anuncio**: campo nuevo `intended_message` en la campaña («Mensaje que quieres que entiendan», máx. 200c, opcional) y `judgeAdComprehension` en el motor: un juez SIN persona (rúbrica por bandas clonada del `judgeComprehension` de five-second, `DEFAULT_MODEL`) que compara la `perceived_offer` de cada respuesta contra el mensaje pretendido, usando el brief como contexto del juez (su destino legítimo tras retirarlo del prompt del persona en v0.35.0). Solo corre si la campaña define el mensaje; tolerante a fallos. Persiste `comprehension_rate` (columna de la 0019 con fallback a `meta` si está pendiente) y `judge_reasoning` en `meta`; `mean_ad_comprehension` en el summary, como metric del run y como KPI «Comprensión» en resultados (también por respuesta en el drill-down). Scope de telemetría nuevo `campaign_judge`. `createCampaign` con fallback `isMissingColumnError` para `intended_message`.

## v0.38.x · Campañas: robustez operativa (plan CAMPANAS-PLAN-MEJORA, release 4 de 6)

- [x] **v0.38.1 · Presupuesto diario de tokens y estimación de coste previa**: `lib/budget.ts` nuevo (`BudgetExceededError`, `assertBudget`, `budgetGate`) con la env var `SUAAS_DAILY_TOKEN_BUDGET` (tokens por ventana de 24h; sin configurar no hay límite). El gate responde 429 con mensaje de negocio en los 8 endpoints de lanzamiento (`/api/runs/{campaign,five-second,funnel,ab,copy,pricing}`, `/api/geo/run`, `/api/momentum/run`); cierra C-03 de la auditoría. `lib/usage.ts` gana `getTokensLast24h()` y `getScopeAverages()` (medias empíricas de tokens y latencia por scope sobre las últimas 200 llamadas). Nuevo `GET /api/estimate/campaign?campaignId=&profiles=N`: combos, tokens y segundos estimados con el histórico real, más el estado del presupuesto. `ProfileLaunchPanel` gana `estimateEndpoint` (muestra «estimado ~Nk tokens · ~Ns · presupuesto 24h: X% usado» con debounce al cambiar la selección) y `combosPerProfile` (el cap de 200 combinaciones se valida en cliente antes del submit; antes solo reventaba en servidor). Fix de v0.36.0: `ProfileExplorer` gana `initialSelected` para que «Repetir con esta muestra» marque visualmente los checks. Pendiente operativo anotado: comparar dos runs idénticos Opus vs Sonnet con datos de `gateway_usage` antes de decidir el cambio de modelo del probe.
- [x] **v0.38.0 · Runner tolerante a fallos con progreso observable y reanudación**: el orquestador se divide en `prepareCampaignRun`/`prepareCampaignResume` (validación + creación o rehidratación, dentro del request) y `executeCampaignRun` (cola aplanada perfil × canal × query con worker pool de 5, 1 reintento por combinación, deadline interno a 270s y cierre GARANTIZADO: summarize sobre lo persistido, métricas `n_failed`/`n_skipped_deadline` y `done` si hay datos). `POST /api/runs/campaign` responde `{ok, runId}` al instante y procesa en `after()` de `next/server`; el body acepta `resumeRunId` (salta las combinaciones ya persistidas: el upsert idempotente ya existía). `GET /api/runs/campaign?runId=` devuelve status/done/expected. Client component `RunProgress` en la página de resultados: barra con polling cada 5s, refresh al terminar, aviso «Run interrumpido: mostrando N respuestas parciales» y botón «Retomar» si lleva más de 10 minutos en running o quedó en error con parciales. Mata los dos peores modos de fallo: el run zombi en «running» y el trabajo pagado descartado por un único error. `runCampaignTest` queda como wrapper síncrono para el sembrador.

## v0.37.x · Campañas: consolidación de BD (plan CAMPANAS-PLAN-MEJORA, release 3 de 6)

- [x] **v0.37.0 · Migración 0019 de consolidación + tracking `suaas_migrations`**: nueva `supabase/migrations/0019_consolidacion.sql` (idempotente, **PENDIENTE de aplicar a mano en el SQL editor**): (1) tabla `suaas_migrations` con backfill de 0001..0018 y convención de que cada migración registre su propia fila; (2) `drop column if exists channel` (legacy pre-0011); (3) checks de arrays rehechos con `coalesce(array_length(x,1),0)` (con `'{}'` el `array_length` es NULL y el CHECK pasaba, los mínimos nunca se garantizaron); (4) checks de Display (`company_name` ≤ 25c, `long_headline` ≤ 90c, `cta` restringido al set de Google Ads); (5) RLS activado en `campaigns` y `campaign_responses` sin policies (el service role lo salta, la anon key queda deny-all); (6) agrupadas las columnas de releases posteriores para una sola sesión de SQL: `campaigns.intended_message`, `campaign_responses.{comprehension_rate,behavior_class,shown_headlines,shown_descriptions}`. En código: `lib/migrations.ts` (`KNOWN_MIGRATIONS` + `getMigrationsStatus()`), sección «Tracking de migraciones» en `/diag` y campo `migrations_tracking` en `/api/diag`. La poda de los fallbacks legacy pre-0011/pre-0013 queda para cuando la 0019 conste como aplicada.

## v0.36.x · Campañas: loop de iteración (plan CAMPANAS-PLAN-MEJORA, release 2 de 6)

- [x] **v0.36.2 · Comparativa run vs run**: nueva ruta `/campaigns/[id]/compare` que compara dos runs completados (por defecto los dos últimos; selector GET sin JS para elegir otros): dos `CompareCard` con intent, claridad, credibilidad y match landing, cuatro `DeltaBar` y la regla de empate del A/B (diferencias < 0,02 no declaran ganador) sobre el intent medio. Aviso destacado cuando las muestras de perfiles difieren (la diferencia puede ser de muestra, no de anuncio). Los bloques de comparación del A/B (`VariantBlock`, `DeltaBar`, `Bar`, `pickWinner`) se extraen a `components/compare-blocks.tsx` parametrizados (`CompareCard`, `DeltaBar`, `pickCompareWinner`) y `/experiments/ab/[abTestId]` pasa a consumirlos. Botón «Comparar runs» en el detalle si hay 2 o más runs done. Cierra la release v0.36 (loop de iteración).
- [x] **v0.36.1 · Export a CSV y a formato Google Ads Editor**: nueva ruta `GET /api/export/campaign/[runId]` (tras el proxy de auth global). Formato por defecto: CSV humano para Excel en español (BOM UTF-8, separador «;», decimales con coma), una fila por respuesta con perfil, canal, query, los 4 scores, match landing, barreras, oferta percibida, razonamiento y la versión ideal completa. Variante `?format=ads_editor`: CSV con cabeceras RSA (Campaign, Ad Group, Headline 1..15, Description 1..4, Final URL) con una fila «Original» (assets de la campaña) y una fila «SUAAS ideas» (top de versiones ideales rankeadas por intent, dedupe, truncadas a 30/90, completadas con los originales). Botones «CSV» y «Ads Editor» en la cabecera de la página de resultados.
- [x] **v0.36.0 · Duplicar campaña y repetir run con la misma muestra**: `/campaigns/new?from=<id>` siembra el formulario con la campaña original (nombre con sufijo «(copia)», estrategia, brief, queries, headlines, descriptions, campos Display y creatividades reutilizadas por URL, sin resubir nada; la landing reutiliza `landing_image_url` vía `landing_resolved_url`). Botón «Duplicar» en el detalle. `RunsPreviousGrid` gana `repeatSampleBase`: cada run previo ofrece «Repetir con esta muestra» (`?profiles=id1,id2`), el detalle filtra los ids contra los perfiles vivos y `ProfileLaunchPanel` (prop `initialSelected` nueva) arranca expandido con esa selección. Cierra el flujo: duplicar, cambiar una cosa, relanzar sobre los mismos perfiles.

## v0.35.x · Campañas: fidelidad y honestidad (plan CAMPANAS-PLAN-MEJORA, release 1 de 6)

Primera release del plan de mejora del módulo de campañas (`docs/CAMPANAS-PLAN-MEJORA.md`). Cada mejora se publica de una en una con su propio ciclo de deploy.

- [x] **v0.35.0 · El persona ya no ve el brief del anunciante**: `probeCampaignSnippet` (`lib/experiments/campaign.ts`) inyectaba «Nota del anunciante (no la verías tú, sólo contexto)» con el brief en el user content del persona, el sesgo de cámara de eco que prohíbe `CONOCIMIENTO-USUARIOS-SINTETICOS.md` §6.1. Eliminado: el brief queda para la UI y como contexto de futuros jueces neutrales. Los scores de runs nuevos pueden bajar ligeramente respecto al histórico (señal de que el sesgo existía).
- [x] **v0.35.1 · Razonamiento antes del score con anclas verbales**: `SnippetEvalSchema` reordenado (`perceived_offer` → `reasoning` nuevo → `barriers` → scores; zod preserva el orden de claves y `generateObject` genera en ese orden) y el system del probe sustituye los bullets «0..1» por bandas verbales por métrica (0,0-0,2 lo ignorarías … 0,8-1,0 click casi seguro) más la instrucción de usar todo el rango. El `reasoning` se persiste en `campaign_responses.meta` (jsonb, sin migración; `meta` deja de ser write-only), `CampaignResponse` lo expone (`null` en filas antiguas) y el drill-down por perfil lo muestra en cursiva.
- [x] **v0.35.4 · Frontera de BD tipada**: `isMissingColumnError` deja de devolver `true` incondicional con `PGRST204` y comprueba que el mensaje de PostgREST mencione la columna preguntada (antes un error por la columna A disparaba el fallback de la columna B en cadena). `normalizeCampaign` valida con el nuevo `CampaignRowSchema` (zod) en lugar de castear: `channels`/`strategy` con `.catch()` a sus defaults, `creatives` validado elemento a elemento (un jsonb corrupto se descarta sin tumbar el listado y el runner ya no recibe creatividades sin `kind`/`role` validados). El fallback legacy `channel` → `channels[]` se mantiene hasta la consolidación de esquema.
- [x] **v0.35.3 · Honestidad de métricas y errores legibles**: «CTR»/«Click rate»/«Tasa click» pasan a llamarse «Intent ≥ 0,5» (KPI con `InfoTooltip` explicando el umbral, cabeceras de las tablas por canal y por query, y métrica del grid de runs en el detalle). Nota metodológica visible bajo los KPI: el umbral no es comparable con el CTR real, el match de landing solo lo evalúan los perfiles que superan el umbral y se juzga sobre la og:image (`landing_source_url` gana su primer consumidor) o el screenshot subido, y las creatividades de vídeo se evalúan por su miniatura. Se persisten `mean_credibility`, `mean_differentiation` e `intent_stddev` (nuevo en `CampaignSummary`) como metrics del run. En el form de creación: errores de zod legibles (mensajes en castellano unidos con «·» en vez del volcado JSON), pre-validación de `CampaignInputSchema` con URLs provisionales ANTES de subir blobs (un submit de Display incompleto ya no deja blobs huérfanos) y aviso en cliente con los requisitos de Display junto al submit.
- [x] **v0.35.2 · Bugs latentes del runner**: (a) fuera los `.max(30)/.max(90)` de `IdealVersionSchema` (un titular de 31 caracteres del LLM lanzaba `NoObjectGeneratedError` y mataba el run; el límite se instruye en el prompt y se garantiza con `slice` al persistir); (b) `byQuery` se deriva de las queries presentes en las respuestas (no de `campaign.queries`), con constante exportada `GENERAL_CONTEXT_QUERY`, etiqueta legible «Contexto general» en la UI y tabla por query oculta si no hay filas; (c) caché de imágenes por run (`Map<string, Promise<string>>`): la landing y las creatividades se descargan una vez en lugar de una por combinación perfil × canal × query; (d) el catch vacío de la landing eval ahora loguea con contexto y persiste `landing_error` en `meta` (distingue fallo técnico de gating por intent); (e) `profile_id` en el `meta` de los tres `recordUsage`; (f) la estimación de duración del panel de lanzamiento ya no da «~0s por perfil» en Display (queries vacías cuentan como 1 contexto de interés).

## v0.34.0 · Papelera coherente en históricos + MigrationPendingError + diag de migraciones pendientes

Cierre de los flecos de papelera detectados en la auditoría de v0.32: una entidad enviada a papelera ya no rompe sus resultados históricos ni deja errores crípticos cuando falta la migración 0017.

- [x] **Vistas históricas a prueba de papelera**: `lib/{targets,ab,campaigns,copy,funnels,pricing}.ts` exportan variantes `getXWithTrashed` (no filtran `deleted_at`). Las usan `/experiments/{five-second,funnel,ab,copy,pricing,campaign}/[runId]` y `/ab/[id]`: antes una entidad en papelera devolvía 404 en sus resultados históricos. Las vistas ocultan el enlace al detalle cuando la entidad está en papelera (para no enlazar a un 404).
- [x] **Bloqueo de lanzamiento sobre entidades en papelera**: `/ab/[id]` muestra badge «En papelera» en la variante afectada y, si alguna variante está en papelera, sustituye el panel de lanzamiento por un aviso (no se lanzan runs nuevos; los históricos se siguen mostrando). `/geo/[id]` y `/momentum/[id]` aplican el mismo bloqueo y el run se rechaza server-side si la entidad está en papelera.
- [x] **`MigrationPendingError`**: nueva clase exportada en `lib/supabase.ts` con mensaje seguro (solo nombra el archivo de migración). `lib/{geo,momentum,profiles}.ts` la lanzan en softDelete/restore cuando falta la columna `deleted_at` (migración `0017_trash_geo_momentum_profiles.sql`) y `/api/trash/[type]/[id]` la traduce a HTTP 409.
- [x] **Diag ampliado**: `/diag` y `/api/diag` auditan `CRITICAL_COLUMNS` (columna → migración que la añade) sobre varias tablas: las 6 FKs de `runs` (`target_id`, `funnel_id`, `ab_test_id`, `copy_deck_id`, `pricing_offer_id`, `campaign_id`), `deleted_at` en las 9 entidades con papelera, `campaigns.{channels,strategy}`, `profiles.intent_context` y `five_second_responses.behavior_class`. `TABLES` incluye ahora `geo_analyses` y `momentum_challenges`. El JSON renombra `runs_columns`/`missing_runs_columns` a `columns`/`missing_columns` y añade `pending_migrations` (lista deduplicada de archivos `.sql` por aplicar; si falta una tabla entera, incluye la migración que la crea).
- [x] **Menos superficie de API en GEO y Momentum**: borradas `POST /api/geo` y `POST /api/momentum` (la creación usa server actions en `app/{geo,momentum}/new/actions.ts`; solo quedan `POST /api/geo/run` y `POST /api/momentum/run`). Borradas también las server actions de borrado (`app/geo/actions.ts`, `app/momentum/actions.ts`): ambos módulos usan `SendToTrashButton` (`components/trash-button.tsx`) con la nueva variante `inline`.
- [x] **SeedGate compartido**: `seed-gate.tsx` movido de `app/seed-examples/` a `components/`, reutilizado por `/seed-examples` y `/profiles/seed`. El gate de cookie `seed_access` (`SEED_PASSWORD` vía `/api/seed/access`) protege ahora también `GET /profiles/seed` (muestra el gate sin cookie) y `POST /api/profiles/seed` (401 sin cookie).
- [x] **Limpieza de tema**: `#fff` → `var(--text-strong)` y tonos `*-500` de estado → `var(--*-text)` en diag, tokens, import de perfiles, seed y migration-needed. Keyframe `.spin` global en `globals.css`.

## v0.33.0 · Capa de temas (dark por defecto + modo claro) y limpieza anti-AI-slop

Lavado de cara alineado con la spec de sd.michelvalles.com (RULES, ANTIPATTERNS, VOICE, PRODUCT-UI). Dos frentes: sistema de temas conmutable y erradicación del slop visual que la spec prohíbe.

- [x] **Capa de temas en `globals.css`**: canal `--fg` (componentes RGB del primer plano) + `--surface-app`, `--surface-panel`, `--text-strong`, `--accent-text` y semánticos de texto (`--success-text`, `--warning-text`, `--error-text`). Oscuro por defecto en `:root`; `html[data-theme="light"]` invierte. Todo el CSS y los inline styles consumen `rgba(var(--fg), alpha)` en lugar de blancos hardcodeados (791 ocurrencias migradas por codemod en 64 archivos).
- [x] **Switch de modo claro** al pie del sidebar (`components/theme-switch.tsx`): toggle pill con `role="switch"`, persistencia en `localStorage` (`suaas-theme`) y script inline en `app/layout.tsx` que aplica la preferencia antes del primer paint (sin destello). En claro el accent de texto baja a `--accent-700` para mantener contraste sobre paper.
- [x] **Contextos de tema fijo**: `.theme-dark-fixed` para el login HUD y `/onboard` (siempre oscuros por diseño); `.surface-feature` y los tooltips redefinen el canal localmente. Los previews de anuncios (SERP/Display) no cambian: emulan superficies reales.
- [x] **Fuera el arcoíris Tailwind**: la home y `/gravity` usaban `#60a5fa`/`#fb923c`/`#a78bfa` (azul/naranja/violeta-IA) con glow neón para los tres planos. Ahora: numeración editorial `01·02·03` en accent, anillos del diagrama orbital en alphas del canal `--fg`, centro (la intención) como única masa con color y pulso discreto sin glow. Antipatrones de la spec eliminados: AI-purple, outer glow, segundo color saturado.
- [x] **Semáforo de estados con tokens**: `#4ade80`/`#facc15`/`#f87171` (verde/amarillo/rojo Tailwind) sustituidos por `--success-text`/`--warning-text`/`--error-text` en GEO, Momentum, chat panel y test 5s (uso semántico licenciado por el DS, ahora legible en ambos temas).
- [x] **Micro-slop de copy**: em-dashes eliminados de comentarios de `lib/`, glifo `★` retirado de los chips «Ganadora» y «mejor», `color: "#000"` → `var(--ink-900)`.
- [x] El logo del sidebar deja el `filter: invert(1)` inline y pasa a `.sidebar-logo` (invertido en oscuro, original en claro).

Pendiente anotado: la contraseña del bloque «Conceptos pendientes» de `/gravity` sigue hardcodeada en código cliente (antipatrón de PRODUCT-UI.md); moverla a env en el sprint de seguridad.

## v0.32.0 · Consistencia de funcionalidades menores (papelera + seed en todos los módulos, seed con brief)

Auditoría de consistencia módulo a módulo (10 agentes) y cierre de los huecos de papelera y sembrador. Detalle de lo aplicado:

- [x] **Papelera para GEO, Momentum y Perfiles**: migración `0017_trash_geo_momentum_profiles.sql` añade `deleted_at` + índice a `geo_analyses`, `momentum_challenges` y `profiles`. `lib/geo.ts` y `lib/momentum.ts` ganan el trío softDelete/restore/hardDelete y sus listados filtran `deleted_at is null` (con fallback `isMissingColumnError` si la migración no está aplicada). `lib/trash.ts` pasa de 6 a 9 `TRASH_TYPES`; `/trash` los agrupa y resume. Los botones de borrar de `/geo`, `/momentum` y `/profiles` ahora envían a papelera (antes borraban en duro) y sus confirms lo explican.
- [x] **Perfiles: soft delete como red de seguridad**: el borrado duro de un perfil destruye en cascada todos sus runs y respuestas históricas (`on delete cascade`). Ahora `DELETE /api/profiles/[id]` hace soft delete; el borrado definitivo solo es posible desde `/trash`. `listProfilesByIds` NO filtra `deleted_at` a propósito: los resultados históricos siguen mostrando el perfil aunque esté en la papelera.
- [x] **Seed para GEO y Momentum**: `seedGeoExample` (visibilidad de Flat 101 con 3 segmentos JTBD) y `seedMomentumExample` (Trigger de urgencia dental, asigna 3 perfiles aleatorios aunque no se lance). Kinds `geo` y `momentum` en `/api/seed/examples` y tarjetas en `/seed-examples` (ahora «los 8»). Con `launch > 0`, GEO ejecuta `runGeoAnalysis` y Momentum `runMomentumChallenge`.
- [x] **Seed con brief**: campo opcional en `/seed-examples`. `lib/seed-brief.ts:generateSeedPlan(brief, kinds)` hace UNA llamada `generateObject` que produce contenido coherente (misma marca y sector) para todos los módulos seleccionados; cada `seedXxxExample` acepta ese plan en lugar de sus defaults estáticos. Los módulos con imagen piden al modelo URLs reales de marcas conocidas del sector (og:image se resuelve en runtime y el fallo queda aislado por módulo). Saneado de límites duros (headlines 30 chars, descriptions 90, arrays acotados) tras la generación. Scope de telemetría nuevo `seed_brief`. Sin brief, todo funciona exactamente como antes.
- [x] **Fix drift zod↔SQL en campañas**: el check de `descriptions` exigía 2..4 en SQL pero zod permite 1..5 (Display). Migración `0018_campaigns_descriptions_fix.sql` (idempotente) lo corrige y añade el índice `campaigns_deleted_at_idx` que faltaba desde 0008.
- [x] **Higiene de errores en GEO y Momentum**: `/api/geo`, `/api/geo/run`, `/api/momentum` y `/api/momentum/run` pasan a `lib/error-response.ts` (antes filtraban mensajes internos de Supabase al cliente). Nota: ambos endpoints de creación (`/api/geo` y `/api/momentum`) se eliminaron en v0.34.0 en favor de server actions. Los endpoints de run conservan una lista blanca de mensajes de negocio propios («El análisis ya está en marcha», etc.) que devuelven 409. `/api/momentum` ahora valida con zod (incluye `profile_ids` min 1, alineado con el form). `createGeoAnalysisAction` captura errores del insert en lugar de reventar el form.
- [x] **CSV de perfiles conserva `intent_context`**: la columna se añade a `PROFILE_CSV_HEADERS` y a `profileToCsvRow`; el ciclo exportar → importar ya no pierde el JTBD (retrocompatible con CSVs antiguos: la columna ausente queda vacía).
- [x] **Comentarios y avisos desactualizados**: entity-list/entity-card/trash-button ya no hablan de «5 entidades»; los avisos de `/trash` citan 0007 y 0017.

Pendientes detectados por la auditoría y aplazados (candidatos a próximos sprints):
- Edición/renombrado tras crear (solo profiles tiene update; duele especialmente en campañas y embudos) y duplicar/clonar entidades.
- Acoplamiento papelera ↔ A/B: el hard delete desde `/trash` borra en cascada sin avisar de las referencias que destruye. Falta chequeo de referencias. (La parte del 404, un target en papelera rompía el detalle del A/B y los resultados históricos, quedó resuelta en v0.34.0.)
- Botón de papelera en las páginas de detalle (hoy solo en las cards de listado, en todos los módulos).
- Factorizar el componente `Notice` de error duplicado en 7+ páginas.
- Runners de GEO/Momentum síncronos dentro del POST (riesgo de timeout y status `running` huérfano; ya señalado en `AUDITORIA-SEGURIDAD.md` B-01/B-03). Mitigado en GEO desde v0.56 (`maxDuration = 300` + segmentos en pares), pero el patrón sigue siendo síncrono.
- Unificar los dos patrones de UI de borrado que quedan (SendToTrashButton vs `DELETE /api/profiles/[id]` en el explorer de perfiles). Las server actions de borrado de GEO y Momentum se eliminaron en v0.34.0.

## v0.30.0 · Módulo Momentum (Intent Momentum ante-touchpoint)

- [x] **v0.30.0**: nuevo módulo `Momentum`. Define **escenarios de activación** (retos JTBD) y simula cómo cada perfil abordaría ese reto en su vida real, antes de que ninguna marca entre en su radar. Para cada perfil extrae: narrativa en primera persona, intensidad (0-1), dirección (approaching/stable/drifting), velocidad (accelerating/steady/decelerating), primeros pasos ordenados, canales que usaría y barreras. El resumen agrega la distribución de dirección, intensidad media y top 5 canales más frecuentes. Migración `0016_momentum.sql` añade la tabla `momentum_challenges` con campos `trigger_scenario`, `brand_context` (opcional), `profile_ids[]` y `results` jsonb. Nuevo scope de telemetría `momentum_probe`. Entrada `Momentum` (icono Zap) en el sidebar.
   - Aplicar `0016_momentum.sql` en Supabase antes de crear el primer reto.

## v0.29.0 · Gravity Model (4 funcionalidades)

Cuatro nuevas funcionalidades inspiradas en el framework Gravity Model para modelar la intención de los usuarios como un vector (intensidad, dirección, velocidad).

- [x] **Contexto JTBD en perfiles**: campo `intent_context` (texto libre) en `profiles`. Se inyecta en el system prompt de todas las llamadas que usan ese perfil. Permite segmentar por trabajo-a-hacer en lugar de por demografía. Visible en el formulario de creación y edición de perfiles bajo "Contexto de intención (JTBD)".
- [x] **Intent Momentum en el chat**: el `ReasonerPlanSchema` gana un objeto `momentum` con tres dimensiones: `intensity` (0..1), `direction` (approaching / stable / drifting) y `velocity` (accelerating / steady / decelerating). El `ChatPanel` muestra un `MomentumIndicator` visual bajo el bloque de razonamiento colapsable.
- [x] **Clasificación de conducta en el test de claridad 5s**: `ProbeOutputSchema` añade `behavior_class` (optima / fuga / repesca). El LLM clasifica su propia conducta durante el probe (sin llamada extra). Se persiste en `five_second_responses`, se muestra como badge en la tabla de respuestas y se añade un bloque de distribución en la página de resultados del run.
- [x] **GEO tester (Generative Engine Optimization)**: módulo nuevo en `/geo`. Simula cómo un buscador IA (Perplexity / Google AI Overview / ChatGPT Search) describe la marca ante cada segmento de intención. Produce `visibility_score`, `brand_position`, `recommendation_tone`, `key_claims` y `missing_attributes` por segmento. Tabla `geo_analyses` (jsonb para segments y results). Rutas: `/geo` (lista), `/geo/new` (form con JSON de segmentos), `/geo/[id]` (resultados con resumen y detalle por segmento + botón Analizar). API: `POST /api/geo/run`.

Aplicar migración `0015_gravity_model.sql` en Supabase antes de usar estas funcionalidades.

## v0.28.0 · Auditoría de seguridad y robustez

- [x] **v0.28.0**: auditoría completa de seguridad (ver `docs/AUDITORIA-SEGURIDAD.md`). Cuatro correcciones críticas/altas:
  - **VULN-01 (crítica)**: `isSupabaseConfigured()` reescrita para no depender de `NEXT_PUBLIC_SUPABASE_ANON_KEY`. La anon key ya no se incluye en el bundle del navegador, eliminando el vector de acceso directo a Supabase sin RLS desde el cliente.
  - **VULN-02 (alta)**: `lib/image-source.ts:resolveImageForApi` llama a `assertPublicUrl` antes de hacer fetch de URLs http(s). Cierra el SSRF en descargas de imágenes multimodal (creatividades y landing de campañas, imágenes de targets y pasos de embudos).
  - **VULN-03 (alta)**: `/onboard/result/[id]` y `/api/onboard/og?id=<uuid>` verifican `profile.source === 'self_report'`. Antes, cualquier perfil (manual, llm_seed) era accesible en esas rutas públicas conociendo su UUID.
  - **VULN-04 (alta)**: `lib/onboard.ts` ya no filtra el mensaje de error del LLM al usuario no autenticado; emite `"Error al procesar. Inténtalo de nuevo."` y registra el detalle en `console.error`.
  - Documentadas en el informe: sin rate limit en /api/auth (VULN-05), rate limit en memoria no cross-instance (VULN-06), /api/qr sin validar content (VULN-07), DNS TOCTOU (VULN-08), race condition en upsertMetric (VULN-09).
  - Corregida discrepancia de scope `seed_profile` vs. `reasoner_chat` en docs (STAB-05).

## v0.27.x · Onboard público: humano real → gemelo sintético

- [x] **v0.27.3**: copy de la portada y del resultado del onboard pasa de «Lo usamos en tests de UX y CRO» a «Lo usamos en estrategias de captación, creatividades y UX». Refleja mejor que los gemelos sirven para validar paid ads, copy y creatividades, no sólo tests de usabilidad.
- [x] **v0.27.2**: la pantalla del wizard cabe en el viewport visible del móvil sin scroll. `layout.tsx` y `SubmittingScreen` pasan de `100vh` a `100svh` (small viewport height descuenta las barras del navegador: barra de URL en Safari iOS, barra de Chrome Android). Padding general reducido con `clamp(14px, 4vw, 48px)`. Antes el footer con el botón «Siguiente» quedaba debajo de la línea visible y obligaba a hacer scroll para pulsarlo.
- [x] **v0.27.1**: review del wizard detecta preguntas incompletas y muestra un panel con lista clicable de cada hueco («Pregunta 7 · Tener mucho dinero…», «Pregunta 30 · Ocupación y día típico»). Botón principal pasa a llamarse «Tienes N preguntas sin responder» en gris cuando hay huecos y al pulsarlo salta automáticamente al primer paso faltante en lugar de enviar a medias. Reemplaza el error server-side genérico «Cuestionario incompleto» por feedback inline accionable. Helper `findMissing()` central + label humano por paso vía `stepLabel()`.
- [x] **v0.27.0**: nueva ruta pública `/onboard` (fuera del gate de `auth_suaas` vía whitelist en `proxy.ts`). Cuestionario híbrido de 31 preguntas (5 demográficas + 24 HEXACO-24 Likert 1..5 + 2 abiertas) presentado como wizard step-by-step con `motion`, progress bar y avance automático en HEXACO. Mapeo determinista HEXACO → OCEAN (Honestidad-Humildad se pasa al LLM como contexto pero no se persiste). El LLM (Opus, scope `onboard_synthesize`) sólo extrae `occupation`, `com_b_barriers` y compone la `backstory` integrando frases TEXTUALES de las dos respuestas abiertas (fidelidad grounded). Pantalla de loading con frases narrativas rotativas vía stream NDJSON. Resultado en `/onboard/result/[id]` con tarjeta visual (avatar de iniciales, OCEAN sparkbars, top 3 barreras COM-B, backstory en cursiva). Botón «Descargar mi gemelo» que pega contra `/api/onboard/og` (PNG 1200x630 generado con `next/og ImageResponse`). Modal «Compartir cuestionario» en la toolbar de `/profiles` con URL + QR (server-side via `qrcode` package, nuevo endpoint `/api/qr`). Anti-abuso: rate-limit en memoria 5/h por IP + tope global 50/día, honeypot field, sin email. Perfiles generados se marcan con `source='self_report'` para distinguirlos del seed manual o `llm_seed`. Sin migración (todo encaja en el schema existente).
  - Aprobar la base de conocimiento de [[hexaco-mapping]]: 5 dimensiones compartidas directas (O, C, E→X, A); Neuroticismo = Emocionalidad HEXACO (no inversa porque ambas miden la misma cosa con el signo correcto).
  - Telemetría: scope nuevo `onboard_synthesize` añadido a `UsageScope`. Aparecerá en `/tokens`.
  - Dep nueva: `qrcode` + `@types/qrcode`.

## v0.26.x · Display Ads (RDA)

- [x] **v0.26.4**: `docs/SIGUIENTE-PASO.md` refrescado tras llevar congelado en v0.4.0 muchos sprints. Ahora refleja el estado real (v0.26.3, módulos productivos, decisiones recientes a no romper, próximos candidatos: 3ª estrategia de Google Ads / multi-canal real / datasets reales para perfiles).
- [x] **v0.26.3**: subtítulos (`description=` del `PageHeading`) eliminados de **todas las páginas índice y new** (listados de claridad, embudos, ab, copy, pricing, campañas, perfiles + sus `/new`, `/edit`, `/seed`, `/import`, `/diag`, `/tokens`, `/trash`, `/seed-examples`). El `description` se mantiene sólo en las páginas de detalle, donde el contenido es el `brief` o la descripción de la entidad. Documentación regenerada con detalle exhaustivo: `docs/PROYECTO.md` reescrito desde cero (árbol completo de carpetas, modelo de datos con todas las tablas, sección dedicada al módulo Campañas con schema, strategies, roles de creatividades, runner, defensa frente a migraciones pendientes y seguridad activa), `docs/SISTEMA-DISENO.md` añade espaciado del shell, panel 3x3 de la home, ChannelIcon/StrategyIcon, patrón pestañas con `Próx.` y patrón de preview en vivo, `docs/DESARROLLO.md` lista las 14 migraciones en orden y nuevas entradas de troubleshooting (seed password, /campaigns 500, headlines check, combinatorial cap, imagen Display preview).
- [x] **v0.26.2**: `.app-shell-main` ahora es `display: flex; flex-direction: column; gap: clamp(32px, 4vw, 56px)`. Antes los `<section>` y `<ol>` aparecían pegados al PageHeading; ahora todas las páginas heredan ese gap automáticamente. Las que envuelven todo en un wrapper flex propio (home, `/trash`) no se ven afectadas (el gap aplica entre hijos directos del main, no entre nietos).
- [x] **v0.26.1**: el preview en vivo de `/campaigns/new` ahora se adapta a la strategy. Para **Display** se renderiza un mockup tipo card con imagen landscape (1.91:1) arriba, logo square + nombre de empresa + URL display debajo, titular largo en grande, headline corto en azul, descripción y botón CTA con color accent. Si aún no has subido landscape o logo se muestran placeholders. Para **Search** sigue mostrándose el SERP textual de antes. Eyebrow del preview muestra la strategy.
- [x] **v0.26.0**: segunda estrategia implementada dentro de Google Ads, **Display (Responsive Display Ad)**. Modelo extendido con `company_name` (max 25c), `long_headline` (max 90c) y `cta` (de un set predefinido), columnas en `campaigns` añadidas por la migración `0014_campaigns_display.sql`. Cada `Creative` gana un campo `role` (`landscape_image | square_image | portrait_image | logo_square | logo_landscape | video_youtube | generic`) que la UI usa para validar los assets obligatorios de RDA.
  - Schema zod ahora usa `superRefine` por strategy:
    - **Search**: queries ≥1, headlines 1..15, descriptions 2..4 (igual que antes).
    - **Display**: company_name y long_headline obligatorios, headlines 1..5 (30c), descriptions 1..5 (90c), creatives con al menos 1 landscape + 1 square + 1 logo_square. Queries opcionales (se interpretan como intereses).
  - Form `/campaigns/new` con branch por strategy: si seleccionas Display, aparece el campo «Nombre de empresa» en Identidad, sección dedicada «Titular largo», sección «CTA» con select, y cada creatividad expone un selector de Rol con los 6 valores. Caps de las secciones de titulares y descripciones se adaptan automáticamente.
  - Runner: `probeCampaignSnippet` renderiza un banner Display (titular largo + titulares cortos + descripciones + CTA + company name) en lugar del SERP cuando `campaign.strategy === "display"`. `framingByChannel` adopta un hook específico de Display («estás leyendo un artículo en una web cualquiera, aparece este banner patrocinado»). Si `queries` está vacío (display sin intereses definidos), el runner usa un placeholder de contexto general.
  - Detalle de la campaña muestra un bloque «Empresa» con company_name, long_headline y CTA cuando es Display, y el eyebrow refleja la estrategia. Las secciones existentes (titulares, descripciones, creatividades, landing) siguen funcionando para los dos modos.
  - Aplicar `0014_campaigns_display.sql` en Supabase + `NOTIFY pgrst, 'reload schema';` antes de crear campañas Display.

## v0.25.x · Estrategias dentro del canal

- [x] **v0.25.0**: dentro de Google Ads aparecen las 7 estrategias publicitarias como sub-pestañas: **Search · Display · Performance Max · Demand Gen · Video / YouTube · App Campaigns · Shopping**. Sólo **Search (RSA)** está implementada; las otras 6 son sub-pestañas con su icono lucide propio (`Image`, `Sparkles`, `TrendingUp`, `Play`, `Smartphone`, `ShoppingBag`) y badge `Próx.`. Al seleccionarlas, el form muestra un panel `En construcción` con la descripción exhaustiva de los campos que tendrá cada una (URLs, caps de caracteres, formatos de imagen, vídeo, CTA, feeds, etc.) en vez de los campos RSA. Helper `isStrategyImplemented(s)` central para activar features futuras sin tocar el form.
  - Modelo: nueva columna `campaigns.strategy text not null default 'search'` con check sobre los 7 valores, migración `0013_campaigns_strategy.sql` idempotente. `lib/campaigns.ts` añade `STRATEGY_VALUES`, `STRATEGY_LABEL`, `STRATEGY_DESCRIPTION` y `Campaign.strategy: Strategy`. `normalizeCampaign` defensivo para BDs sin la columna (cae a `"search"`). `createCampaign` fallback si la migración 0013 no está aplicada todavía.
  - UI: la sección **Estrategia** aparece debajo de la sección **Canal** en `/campaigns/new`. El detalle muestra dos chips: el de canal (gris) y el de estrategia (acento amarillo). El listado añade columna `Estrategia` y reordena los stats (Canal · Estrategia · Runs · Perfiles).
  - Aplicar `0013_campaigns_strategy.sql` en Supabase + `NOTIFY pgrst, 'reload schema';` antes de crear nuevas campañas.

## v0.24.x · Multi-canal por campaña

- [x] **v0.24.2**: tres correcciones tras feedback.
   - **Migración 0012 idempotente** que reescribe `campaigns_headlines_check` a `between 1 and 15` por si la 0009 nunca se aplicó (BDs creadas entre v0.21 y v0.22 conservaban el check original `between 3 and 15` y rechazaban campañas con menos de 3 titulares).
   - **Form revierte a single-channel**. El multi-canal técnicamente sigue soportado en la BD (`channels text[]` con backfill intacto), pero la UI sólo permite Google: las otras 4 pestañas se muestran deshabilitadas con etiqueta `Próx.`. Disclaimer actualizado: «cada red tiene formato propio (caps, creatividades, targeting); Meta, LinkedIn, TikTok y X llegarán como módulos específicos». El payload sigue guardando `channels: [channel]` para mantener consistencia con el schema actual.
   - **Preview lateral** ahora lista todos los titulares y descripciones, no sólo H1 + D1. El SERP simulado conserva H1 + D1 como representativo y debajo un bloque «Otros titulares» / «Otras descripciones» muestra el resto en compacto. Elimina el bloque «Google rota» que ya no aporta porque la información se ve directamente.
- [x] **v0.24.1**: hotfix. `lib/campaigns.ts` añade `normalizeCampaign(row)` defensivo para tolerar BDs aún sin la migración 0011 aplicada (lee `channel` legacy y lo envuelve en `channels: [channel]`; sin nada cae a `["google"]`). `createCampaign` intenta `channels` y, si Postgres responde que la columna no existe, hace fallback a `channel: channels[0]` con un `console.warn` accionable. Evita el 500 en `/campaigns` cuando v0.24 se despliega antes de aplicar el SQL.
- [x] **v0.24.0**: una misma campaña ahora puede simularse en varias redes a la vez. Migración `0011_campaigns_multichannel.sql` convierte `campaigns.channel` (text) en `channels text[]` (1..5 elementos, subconjunto del set de redes válidas), con backfill de las filas existentes. `campaign_responses` añade `channel` y el unique key pasa a `(run_id, profile_id, query, channel)`. El runner itera `profile × channel × query` (en serie por perfil, paralelo entre perfiles), con cap defensivo de 200 combinaciones para no exceder `maxDuration=300`. Persistencia con `onConflict: "run_id,profile_id,query,channel"`. El summary incorpora `byChannel` con las mismas métricas que `byQuery`. UI: form pasa de pestañas single-select a tabs con checkbox e icono por red (mínimo 1, máximo 5), disclaimer dinámico que se adapta según haya search, feed o mixto. Detalle muestra una row de chips por cada canal y el LaunchPanel estima tiempo como `channels × queries × 18s/perfil`. La página de resultados añade tabla «Métricas por canal» (sólo cuando la campaña tenía más de 1) y los chips de canal aparecen en cada response del drill-down y de la sección «como yo lo veo». Listado muestra el primer canal o "N redes" si son más de dos.
   - Aplicar `0011_campaigns_multichannel.sql` en Supabase + `NOTIFY pgrst, 'reload schema';` antes de crear campañas multi-canal.

## v0.23.x · Campañas multi-canal (MVP)

- [x] **v0.23.1**: selector de canal pasa de toggle buttons a **pestañas con iconos** (`components/channel-icon.tsx`). SVGs monocromos (`currentColor`) inline para Google · Meta · LinkedIn · TikTok · X, sin añadir dependencias. La pestaña activa lleva borde inferior accent y peso bold. El detalle de la campaña muestra un chip prominente con icono + nombre de la red junto al botón Volver. Sigue siendo single-select (modelo `channel: Channel` en la BD); si en el futuro queremos multi-canal por campaña, habría que pasar a `channels text[]` con migración.
- [x] **v0.23.0**: el módulo de Campañas deja de ser exclusivo de Google Ads. Nueva columna `channel` (`google | meta | linkedin | tiktok | x`) con default `google` añadida por la migración `0010_campaigns_channel.sql`. `lib/campaigns.ts` exporta `CHANNEL_VALUES` y `CHANNEL_LABEL` y `CampaignInputSchema` valida el nuevo campo. El runner adapta el framing del prompt según red: para Google sigue siendo SERP search activo, para Meta / LinkedIn / TikTok / X se reformula como feed pasivo y las `queries` pasan a interpretarse como intereses / contexto del usuario en lugar de keywords literales. `judgeLandingMatch` y `proposeIdealVersion` también ajustan el hook por red. El form de `/campaigns/new` añade un selector inicial de canal (5 pestañas con etiqueta humana) y muestra un disclaimer dinámico sobre cómo interpretar las queries. El detalle y el listado muestran el canal como chip / stat. Caps de caracteres por red llegarán en una iteración posterior (v0.24+): por ahora todas las redes comparten los caps RSA (30/90) por consistencia.
   - Aplicar `0010_campaigns_channel.sql` en Supabase + `NOTIFY pgrst, 'reload schema';` antes de crear campañas en redes distintas a Google.

## v0.22.x · Campaign form UX + creatividades multimedia

- [x] **v0.22.1**: home rediseñada como panel 3x3. Una sola sección «Panel» con 9 tarjetas (Claridad 5s, Embudos, A/B, Copy, Pricing, Campañas, Perfiles, Tokens, Diag) sustituye las antiguas secciones «Módulos de test», «Agentes y datos» y «Telemetría» finales. Elimina la card de «Talker · Reasoner» como tarjeta suelta (se sigue mencionando en el tutorial). Texto de los 4 pasos ajustado a «seis módulos».
- [x] **v0.22.0**: ronda de mejoras tras feedback de uso del módulo Campañas.
   - **Preview en vivo del SERP** en `/campaigns/new`: panel lateral sticky que renderiza la primera combinación titular + descripción + URL display en formato Google Ads y se actualiza con cada keystroke. Incluye landing resuelta o subida y miniaturas de las creatividades.
   - **Sólo el primer titular es obligatorio** (antes RSA exigía 3 mínimos). Schema zod relajado a `min(1, "Al menos 1 titular.")` y migración `0009_campaigns_relax.sql` que reescribe el check de Postgres a `between 1 and 15`. Los demás titulares aparecen marcados como `opcional` en el form.
   - **Botón «Resolver og:image» funcional**: nuevo endpoint `GET /api/og-image?url=...` que reutiliza `resolveOgImageDetailed` con todas las salvaguardas anti-SSRF. El form llama al endpoint, muestra preview de la landing inmediatamente y guarda la URL resuelta como `landing_resolved_url` en el payload (el action ya no re-resuelve si el front pasó el valor).
   - **Creatividades multimedia**: `Creative` extendido con `kind: "image" | "video" | "youtube"`, `youtube_id` y `thumbnail_url`. La UI muestra tres pestañas por creatividad: Imagen (URL u upload), Vídeo (mp4/webm/mov via URL o upload con tope de 10MB) y YouTube (URL de watch / youtu.be / shorts; el ID se extrae con regex y se previsualiza el embed). Helper `extractYouTubeId(url)` + `youtubeThumbnail(id)` en `lib/campaigns.ts`. En el runner del experimento, las creatividades se filtran por tipo antes de enviarlas al modelo: imagen va directa, YouTube usa el thumbnail jpg, vídeo usa `thumbnail_url` si lo provee el operador (los modelos actuales no procesan vídeo). `lib/blob.ts` ahora acepta también `data:video/*` y `data:audio/*` para uploads.
   - **Quitada la description larga** del `PageHeading` en `/campaigns/new` para dejar más aire entre el header y el formulario.
   - Aplicar `0009_campaigns_relax.sql` en Supabase + `NOTIFY pgrst, 'reload schema';` antes de crear campañas con un solo titular.

## v0.21.x · Campaign Tester (Paid Search RSA)

- [x] **v0.21.0**: nuevo módulo `Campaign Tester`. Migración `0008_campaigns.sql` añade `campaigns` (name, brief, final_url, landing_image_url, queries[], headlines[], descriptions[], creatives jsonb) y `campaign_responses` (snippet eval + landing match condicional + versión ideal estructurada y libre) más `runs.campaign_id`. `lib/campaigns.ts` valida con zod los caps RSA reales de Google Ads (3..15 titulares de 30 chars, 2..4 descripciones de 90 chars, 1..5 queries, 0..6 creatividades). `lib/experiments/campaign.ts` orquesta para cada `perfil × query`: `probeCampaignSnippet` (Reasoner multimodal con la SERP simulada y creatividades anexas) → `judgeLandingMatch` (sólo si `intent_to_click >= 0.5`, Reasoner multimodal con la landing) → `proposeIdealVersion` (Sonnet, propuesta del perfil con misma estructura RSA + texto libre opcional). Caps: 5 queries × 20 perfiles = 100 evals snippet + ~60 landing + 100 ideal en `maxDuration=300`. Telemetría con scopes `campaign_probe`, `campaign_landing`, `campaign_ideal`.
   - **Rutas**: `/campaigns` (lista), `/campaigns/new` (form con preview-RSA en vivo, contador de caracteres y modo URL/upload para landing y creatividades), `/campaigns/[id]` (detalle con snippet preview, queries en chips, titulares, descripciones, creatividades, landing y `ProfileLaunchPanel`), `/experiments/campaign/[runId]` (KPIs globales: intent click / click rate / claridad / credibilidad / diferenciación / match landing; tabla por query; top barreras agregadas; ranking de versiones ideales; drill-down por perfil con su `como yo lo veo`).
   - **Integraciones**: nueva entrada `Campañas` (icono `Megaphone`) en sidebar grupo Producto; `campaign` añadido a `TRASH_TYPES` (papelera), `/diag` audita `campaigns`, `campaign_responses` y `runs.campaign_id`. `lib/seed-examples.ts` incluye `seedCampaignExample` (campaña Vercel · Paid Search agosto con 5 titulares, 2 descripciones y 3 queries reales `hosting next.js / deploy aplicación react / alternativa netlify`); kind `campaign` en `/api/seed/examples` y tarjeta dedicada en `/seed-examples`.
   - Aplicar `0008_campaigns.sql` en Supabase + `NOTIFY pgrst, 'reload schema';` antes de crear la primera campaña.

## v0.20.x · Hardening de seguridad

- [x] **v0.20.0**: paquete de seguridad crítica tras auditoría completa de la app.
   - **Anti-SSRF en og:image** (`lib/url-safety.ts` + `lib/targets.ts:resolveOgImageDetailed`): antes de cualquier fetch server-side iniciado por el usuario (modos URL en `/targets/new` y `/funnels/new`) se valida que el host es público: rechaza loopback, RFC1918, link-local, CGNAT, metadata cloud (169.254.169.254), multicast y direcciones IPv6 equivalentes. Resuelve DNS y revalida todas las IPs devueltas (defensa contra DNS rebinding trivial). Sigue redirects manualmente (`MAX_REDIRECTS=3`) revalidando cada salto y lee el body con tope de 1.5MB y `AbortSignal` con timeout de 5s. Antes el atacante con login podía sondear localhost, IPs internas y endpoints metadata, además de consumir respuestas HTML arbitrarias.
   - **SEED_PASSWORD obligatoria en producción** (`lib/seed-auth.ts`): el fallback `michel101` ahora sólo aplica en `NODE_ENV !== "production"`. En producción `getSeedPassword()` devuelve `null` si la env no está definida y `verifySeedPassword` rechaza cualquier intento. `/api/seed/access` responde 503 con `code: seed_password_unset` para que el `SeedGate` muestre un aviso accionable en vez de "contraseña incorrecta". Acción requerida: añadir `SEED_PASSWORD` en el dashboard de Vercel (producción + preview) o el seed quedará bloqueado.
   - **No filtrar `e.message` al cliente** (`lib/error-response.ts` + endpoints `/api/runs/*`, `/api/chat`, `/api/profiles/[id]`, `/api/trash/[type]/[id]`): mensajes de error 500 devuelven sólo "Error interno." al cliente; el detalle (stack, cause, mensaje original) queda en `console.error` visible en Vercel logs. Los 400 (validación zod) sí mantienen el mensaje porque viene del schema del body que envía el cliente y es seguro. El streaming NDJSON de `/api/chat` también higieniza el frame final de error. `/api/seed/examples` mantiene mensajes detallados al estar detrás de doble auth (login + seed pass).

## v0.19.x · Sembrar como ruta de primer nivel + gate

- [x] **v0.19.0**: entrada **Sembrar** (icono `Sprout`) en el sidebar (grupo Sistema) apuntando a `/seed-examples`. Antes la ruta sólo era accesible por URL directa. Doble gate de acceso: `/seed-examples` y `/api/seed/examples` exigen ahora una contraseña adicional (`michel101` por defecto, override con env `SEED_PASSWORD`) además del login global. Implementado con cookie httpOnly `seed_access` (8h), helper `lib/seed-auth.ts` con `timingSafeEqual` y endpoint `/api/seed/access` (POST/DELETE). Componente cliente `SeedGate` muestra el formulario cuando falta la cookie. Razón: sembrar consume tokens del gateway, conviene una segunda barrera intencional.
- [x] **v0.19.1**: añadido el 5º seed faltante (Claridad 5s). `seedFiveSecondExample` crea un target individual (`Linear · purpose built for product development`) con `og:image` resuelto en runtime. Nuevo kind `"clarity"` en el endpoint, tarjeta dedicada en `/seed-examples` y link a `/experiments/five-second/[runId]` cuando se lanza con `launch > 0`. Antes el sembrador sólo cubría 4 de los 5 módulos (faltaba el más antiguo de todos).

## v0.18.x · Seeds por módulo

- [x] **v0.18.0**: `/api/seed/examples` acepta `kinds: ('copy'|'pricing'|'ab'|'funnel')[]` opcional para sembrar sólo un subconjunto (sin `kinds` o vacío sigue sembrando los 4, compatible con consumidores previos). `/seed-examples` muestra un botón "Sembrar sólo este" en cada tarjeta (Copy / Pricing / A/B / Embudo) además del botón global "Crear los 4". Útil para regenerar un ejemplo concreto sin tocar los demás.

## v0.17.x · Descripción destacada y relanzamiento de runs

- [x] **v0.17.0**: nuevo `descriptionVariant="panel"` en `PageHeading`. La descripción se renderiza como caja destacada con borde-izquierdo accent. Aplicado en las 9 páginas que muestran copy descriptivo (4 detalle + 5 results). Eyebrows de results enriquecidas con metadata (status · N perfiles · contexto). Sweet spot de pricing en layout horizontal para coherencia.
- [x] **v0.17.0 (feat)**: relanzar run desde la página de resultados (panel de lanzamiento de cohorte sobre la misma entidad).
- [x] **v0.17.1**: fix en `/api/chat` aceptando `runId: null` además de `undefined` (cliente envía null al iniciar sesión).
- [x] **v0.17.2**: `ProfileLaunchPanel` colapsado por defecto (CTA "Lanzar nueva Run"), botón arriba al expandir. Fuera de `/experiments/*` por defecto.
- [x] **v0.17.3**: `next.config.ts` con `experimental.serverActions.bodySizeLimit = "10mb"` (default de Next era 1MB y rompía uploads de >700KB de imagen). Más aire en cards de bloques (copy), pasos (funnels) y variantes (ab). `RunsPreviousGrid` y `ProfileLaunchPanel` con padding/gap más generosos.

## Backlog / decisiones abiertas

- ¿Auth por email (Supabase Auth) además del password global? Cuando se invite a clientes externos.
- ¿Caché de respuestas LLM en Vercel Runtime Cache para abaratar iteración?
- ¿Generación de perfiles desde datasets reales (LifeSnaps, Project Baseline)?
- ¿Vercel Queues para encolar runs largos en background?
- Migrar uploads de imagen a **client upload directo a Vercel Blob** (`handleUpload`) para evitar pasar por server actions, en caso de imágenes > 10MB.
